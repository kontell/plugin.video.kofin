"""The generated node trees, smart playlists and skin props (P2.1).

``fs`` is the only module here that deletes anything; ``video`` writes the
video node tree, ``music`` the music one, ``props`` publishes the window
properties skins read. ``kofin.sync.views`` orchestrates them and keeps the
view table, the server listing and the regeneration hash.
"""
