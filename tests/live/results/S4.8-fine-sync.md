# S4.8 — SyncPlay fine sync through inputstream.tempo (run 2026-08-22)

Three Piers members on `jelly.konell.xyz` (Jellyfin 10.11.11), all 22.0-BETA1, kofin 0.21.0, inputstream.tempo 22.4.0, display clock **off** on every box: the desktop flatpak Kodi (**A**, x86_64, queue 4 s), the Galaxy Tab S5e (**B**, 192.168.1.150, armv7, queue 4 s) and the Bravia 4K AE2 "Sokoni" (**C**, 192.168.1.198, armv7, queue **16 s**, 50 Hz panel). Asset: the 10-minute 1080p h264/AAC timecode film from the inputstream.tempo rig, in the Recordings library as *SyncPlay Timecode (2026)* (direct play on all three). Driver: `tests/live/syncplay_fine_sync.py`, which acts as each member's own session against the server and samples positions over JSON-RPC at 4 Hz. Five runs; the numbers below are from run 5 unless stated, runs 1–4 being what shaped the code (§3).

**Verdict: PASS.** Every member is routed through the add-on, pulses land within a few percent of what they promise on all three platforms, a member perturbed by ±150 ms is back inside the deadband within one or two pulses with the others untouched, a 1 s step is closed by one seek plus one pulse, a group pause cuts a running pulse, and Kodi 22's queue goes 4/4/16 s → 1 s at join and back at leave.

## 1. What each scenario showed

| scenario | A−B median (ms) | A−C median (ms) | what happened |
|---|---|---|---|
| steady (35 s from the group start) | +403 → +102 at the end | +126 → 0 at the end | B and C each started behind after the resume, took one corrective seek and one or two saturated pulses, and were on the group inside ~50 s (B) / ~25 s (C); A needed one +82 ms pulse |
| inject +150 ms on A (1.2× for 0.75 s) | +89 before → −24 after | +89 → +50 | A pulsed −75 then −120 (moved −79 / −126); B and C issued nothing in reaction |
| inject −150 ms on A (0.8× for 0.75 s) | +53 → +70 | +18 → +23 | A pulsed +105 then +82 (moved +110 / +86); B, C quiet |
| inject +1 s on A (1.5× for 2 s) | +62 → −133 | +104 → −31 | A: `−938ms is beyond the pulse budget: seeking`, then one saturated pulse (+377 wanted, +313 moved) inside the seek blackout |
| pause during a pulse, unpause 4 s later | +89 after 20 s | +90 after 20 s | A: `cut by Pause`, tempo file 1.0; after the resume every member aligned (+749 / +596 / +518 ms residuals — the normal resume latency), then one pulse each |
| leave | — | — | `videoplayer.queuetimesize restored to 4.0s` (A, B), `16.0s` (C); property cleared; files read 1.0 |

Pulse accuracy across the five runs, `moved` against `wanted`, head-counter measurement (§3): 58 pulses, every one within −5 %…+8 % of its target on all three boxes, e.g. +201/+191 (A), +252/+244 (B), +223/+216 (C). The add-on applied a write within **52–314 ms** (median ~150 ms) on all three — the 250 ms poll plus the demux cadence.

Steady-state pairwise spread once converged, run 4 (sampled 4 Hz, single-sample spikes from RPC timing included in p95): A−B median +14 ms, A−C median −49 ms over 35 s; inject-phase "before" windows in run 5: |median| ≤ 104 ms on every pair.

## 2. Platform notes

- **Routing and audio.** `play <id> via DirectStream (tempo)` on every member; `Player.Process(audiodecoder)` = `ff-pcm_f32le` on all three, the Bravia included.
- **The Tab lands seeks badly.** A corrective seek on B restarts ~370 ms after it is issued and lands up to 350 ms *early* (MediaCodec cannot drop to the accurate-seek point: `demuxer seek to 13048`, clock restarted at 12.70 s), and the landing error varies by GOP position. The controller now learns what a seek leaves behind per device (§3) and finishes with pulses, so B converges, but it is the slowest member to do so — ~50 s from a bad start. A follow-up worth doing: when a device's measured landing error is large, prefer two saturated pulses (20 s, exact) over a seek for residuals under ~600 ms.
- **The Bravia** behaves like the desktop: accurate seeks (+594 → one pulse), pulses on target, no buffering with its queue cut from 16 s to 1 s on Wi-Fi over the 3½-minute run. Its panel stayed at 50 Hz throughout (`Display resolution ADJUST: 3840x2160 @ 50.000000`); with the display clock off that only sets cadence, not the rate.
- **Renderer rebuilds.** `CRenderManager::Configure` over run 4 (3½ min, ~8 pulses and 3–4 seeks per member): A 4, B 3 (1 `framerate changed`), C 4 (2 `framerate changed`) — the initial configure, seeks, and Kodi re-detecting 24.7 fps under a 3 % pulse issued soon after a seek, as the add-on rig predicted. No display-mode switch on any box. No `Traceback`, no `[ syncplay/buffering ]` on any member in any run.
- **Followers do not react to each other's pulses.** In every injection scenario the two untouched members issued nothing (B's one +82 ms pulse in run 5's inject-ahead window was its own residual, decided before the injection).

## 3. What the runs changed in the code

- **Run 1:** `Settings.SetSettingValue` answers a bare `true`, not an object — the queue had been shortened but recorded as not, so `queue_secs` was published as 4.0 and nothing was restored. Fixed in `kodirpc._call`.
- **Run 1:** `moved` read ~15 % low on every pulse. The add-on's `delta_ms` is the Δ it last *reported*, a queue depth behind the head; the head counters (`content_ms − output_ms`) are exact. `tempo.head_delta`.
- **Run 1:** the Tab pulsed ±50 ms against its own read jitter. Deadband 50 → 75 ms, window 2 → 3 s.
- **Run 2:** the Tab sat +400–600 ms behind after every corrective seek. The seek-cost estimate was measured against a 2 s landing tolerance, which a 400 ms correction satisfies before the seek is even processed, so it had decayed to ~150 ms while the Tab's real restart is ~370 ms plus a 350 ms early landing. Now `seek_lag_ms` = restart time − landing offset, timed from the clock restarting (`_clock_restart`), learned from every playing seek, used by both the scheduler's seek and `_align_after_resume`.
- **Run 3:** a seek that lands off-target left the member waiting out the 30 s seek blackout. Pulses now run inside the blackout, saturated at 3 % for 10 s.
- **Run 4:** two members measured +3.3 s after the group Unpause. My regression: the Unpause choreography seeks while still paused, and the clock-restart measurement polled a frozen clock for its whole budget before the resume went out. Measurement only for seeks issued while playing; budget 1.5 s.

## 4. Not covered

- The give-up path (display clock on, mismatched panel) was unit-tested, not run live.
- The crash-restore of `videoplayer.queuetimesize` (hidden `syncPlayQueueRestore`) was unit-tested, not forced live.
- A transcoding member in the group (command-only, by design) and an Omega member (fixed 8 s queue).
- A/V lip-sync across members under a pulse — the photo/beep pair from the add-on rig.

## 5. Day two (2026-08-23): the Pixel, real videos, higher rates, and a bug in the add-on

Members: the desktop flatpak (A), the Tab (B), the Bravia (C) and the **Pixel 7 Pro** (P, 192.168.1.218, arm64, Kodi 22.0-BETA1 20260802, queue already at 1.0 s). Titles from the library rather than the rig asset: *Das Boot – The First Attack* (720p h264, AC3 5.1, 52 min), *Last Train from Gun Hill* (1080p HEVC SDR, E-AC3, 94 min) and, for AV1, the downloaded *Frasier S06E08* (1080p AV1 + Opus).

**The Pixel's "freeze" was a wedged AV1 hardware decoder, and the cause was in the add-on.** Isolated on the Pixel with a `.strm` through the add-on and no kofin in the path: native seeks clean; add-on at 1.0× clean; add-on at 1.03× + seek → `dequeueInputBuffer failed` storm within a second, picture frozen (or corrupt) while the clock ran on — and a second round **watchdog-rebooted the phone** (`sys.boot.reason=watchdog`). The Bravia's AV1 decoder took the identical sequence at 1.03× and 1.25× without a mark. On the desktop the same seeks on HEVC logged ~46 `[hevc] Could not find ref with POC` errors per seek through the add-on at 1.0× and 1.03× alike, and none natively — the decoder was being started mid-GOP. Cause: `SeekTime`'s probe for where a seek landed read the first packets of the new position and *freed* them (inherited from upstream ffmpegdirect; Kodi's own demuxer keeps its probe packet with `ReadInternal(keep=true)`). inputstream.tempo 22.4.1 / 21.4.1 queue them and deliver them first; with it the desktop's HEVC post-seek errors are **0** at 1.0× and 1.03×, and the Bravia's AV1 is still clean. Re-exposed with 22.4.1 installed, the Pixel passed: AV1 through the add-on at 1.03× (slope 1.032) and 1.25× (1.265), two seeks at each rate and one at 1.0×, no decoder errors, picture moving, phone up — so the AV1 toggle that existed for a day was removed and every codec is routed.

**HEVC, all four devices, 1.03 / 1.10 / 1.25 held, seeks at each, a 1.0→1.25→1.0 step with a seek inside** (`.strm`, add-on only): Pixel slopes 1.026 / 1.098 / 1.276, Tab (h264) 1.031 / 1.122 / 1.246, desktop 1.022 / 1.099 / 1.273, all seeks clean, picture moving, no decoder errors, no mode switch, one `framerate changed` reconfigure at 1.25× on the desktop and none on the phones; the Bravia the same with its slopes lagging one phase behind the writes (its 16 s queue outlasts the measurement window). The Tab's one HEVC open coincided with a Kodi restart that left no crash trace; it opened the same stream natively and through the add-on afterwards without incident.

**Real-video groups (four members).** h264 episode: the Tab transcoded (its profile lacks AC3 5.1 direct play) and rode the command-only path; the other three were routed. With the 2.5 s budget and 25 % ceiling, a +1 s injection on A closed with one ramped 0.813× pulse (`moved −978 (wanted −934)`) — no seek — and after a pause/resume and a group seek the Bravia and Pixel each fell ~2 s behind (the Android pipelines restart slowly on a 5.1 track) and were pulled back by one ramped 1.25× pulse each (`+1912 (wanted +1863)`, `+1979 (wanted +2027)`), every pair inside ±90 ms median afterwards. HEVC: the desktop and Bravia routed and in sync through the same scenarios (a −1062 ms ramped pulse at the start, ±60 ms after the resume); both phones transcoded it (`TranscodeReasons=AudioCodecNotSupported`, E-AC3).

Two scheduler defects found and fixed on the day: the give-up test read a 1 s injection as "−160 000 ppm regrowth" and stopped a member for the item (now: regrowth must be steady, plausible and agree across pulses; any command or seek is a fresh start); and a residual straddling the budget — median beyond it, window not all beyond it — got neither a seek nor a pulse (the Bravia sat at +2.5 s for 25 s); now anything short of a unanimous seek is pulsed.

Housekeeping: JSON-RPC setting writes are not saved to disk by Kodi, so the queue-restore record is written before the queue is shortened and the shortening is skipped if it did not stick; the Pixel had been found at 1.0 s with no record.
