# plugin.video.kofin — testing plan

Date: 2026-07-16. Companion to `rewrite-research.md`; scenario IDs map to its build phases (§11). Live testing drives the Kodi instance on this box with the `kodi-drive` tooling (`~/bin/kodi-remote`, `kodi-builtin`, `kodi-shot`, `kodi-diff`, `kodi-logtail`; web server `localhost:8080` `kodi:kodi`, EventServer UDP 9777).

---

## 1. Test environment

* **Kodi**: this box runs Omega (`~/.kodi/userdata/Database/MyVideos131.db` present). Kodi must be running for live tests; the JSON-RPC ping (`Application.GetProperties`) is the readiness check. Piers coverage is schema-level (L2) until a Piers install exists — revisit when the box upgrades.
* **Jellyfin**: a dedicated **test user** and dedicated **test libraries** on the server, never the real ones. Mutating scenarios (delete, rename, watched-flips, retention games) are restricted to test libraries built from expendable media — tiny ffmpeg-generated files (`ffmpeg -f lavfi -i testsrc=duration=90 …`) named to scrape as: ~30 movies (some in a collection, two with `extras/` special features, one multi-version), ~10 shows / ~200 episodes (one show with specials, one with theme/extras), a small music library, a few music videos. Server address is configurable per run (LAN or `jelly.konell.xyz`); one segments-capable show (Intro Skipper-analyzed or chapter-based) is needed for S3.x.
* **Server plugins**: official KodiSyncQueue installed from the catalog (tier-2 scenarios); KofinSyncQueue side-loaded when phase 5 lands (tier-1); both removable for tier-3.
* **Peer addons**: `service.upnext` is **not** used — kofin owns the Play Next dialog off the segment tick (S3.3, plan §8.2), so no upnext install is required on the box; the old `plugin.video.jellyfin` (kontell fork build) installable for the A/B equivalence scenario.
* **Assertion surfaces** (in trust order): sqlite3 on `MyVideos131.db`/`MyMusic83.db`/kofin's `jellyfin.db`; Kodi JSON-RPC; Jellyfin REST (`/Sessions`, `/Items`, `/UserItems/…`); the addon's own debug log via `kodi-logtail`; screenshots last — **pixels prove rendering, never state** (skill rule).

## 2. Test layers

* **L0 — static**: `black --check`, `mypy`, flake; tox config carried from the fork. Every commit.
* **L1 — unit (pytest, no Kodi)**: fork's test suite ports with the sync/syncplay code; Kodi API stubbed the way the fork does it (typings + stub modules). New-code targets: change-feed provider ladder (record parsing for all three tiers, optional-field handling), Etag skip decisions, priority ordering (userdata → removals → added newest-first → metadata → image-only), ISO-8601 parser (7-digit fractions), masking function (token/user-id/device-id never survive), IPC message registry, settings diff engine (whitelist add/remove plans), device-profile builder (codec/HDR/bitrate matrices → expected DirectPlay/CodecProfiles/TranscodingProfiles JSON).
* **L2 — writers against real schemas (pytest + sqlite, no Kodi)**: create pristine `MyVideos131`+`MyMusic83` (Omega) and `MyVideos146`/`MyVideos147`+`MyMusic84` (Piers) databases from Kodi's own DDL (generated once from `ref/kodi-omega-full` / `ref/kodi-piers-full`, checked into `tests/fixtures/`). Run writer fixtures for every media type plus extras assets (asserting `videoversion.itemType` **1 on Omega, 2 on Piers**). Invariants: idempotency (write twice → byte-identical dump), removal leaves zero orphans in any link table, unknown future schema number → writers refuse and surface the "sync paused" state.
* **L3 — live integration**: scenario scripts in `tests/live/` (bash/python wrapping the kodi-drive helpers + curl + sqlite3), each printing PASS/FAIL with evidence paths. Manual only where judgment is needed.
* **L4 — UX review**: `kodi-shot` gates with `kodi-diff` against approved baselines (clock region already masked by the tool).

## 3. Live-test conventions (kodi-drive rules, made binding)

1. Install the dev tree to `~/.kodi/addons/plugin.video.kofin`, then `kodi-builtin 'UpdateLocalAddons()'`; new addons register **disabled** — enable via `Addons.SetAddonEnabled`.
2. Every scenario starts `kodi-logtail mark` and ends `kodi-logtail errors` + `kodi-logtail since` scoped to the action; Kodi debug logging on for request-count assertions (the addon's own debug lines are the grep target).
3. Navigation is blind: screenshot and confirm focus **before** the keypress that matters; `kodi-remote debug` overlay when lost; `ActivateWindow(Home)` for a known start point after reloads.
4. Service bounce = `Addons.SetAddonEnabled` false→true (also exercises our hard-restart path); full Kodi restarts only where unavoidable.
5. Directory/browse tests run headless via `Files.GetDirectory` on `plugin://plugin.video.kofin/...` — no UI navigation needed.
6. **Cleanup is part of the scenario**: each script owns a teardown that removes what it created and asserts the baseline (MyVideos/MyMusic row counts back to pre-test, `addon_data/plugin.video.kofin` removed between suites, any patched files restored). Deletions only ever target test-library media.

## 4. Scenario catalog

### Phase 1 gate — shell (settings, login, browse, playback)

* **S1.1 settings render**: open each tab, screenshot, diff against baseline; every visible setting shows a tooltip; hidden (level-4) settings never render.
* **S1.2 login**: password flow and Quick Connect flow against the test user; wrong-password path surfaces a notification, not a stack trace. Assert `addon_data/…/settings.xml` gained token/userId/deviceId and `isLoggedIn`; assert the raw token string appears **nowhere** in `kodi.log` (masking, with debug logging on).
* **S1.3 account state flips**: after login the Account tab shows server/user read-only + logout/test-connection (screenshot); logout clears hidden creds and flips visibility back.
* **S1.4 browse headless**: `Files.GetDirectory` on the root (views + Add-user + SyncPlay + Settings), a library view, a genres node, a letter node; item properties carry ids/art/resume.
* **S1.5 playback + reporting**: `Player.Open` a movie's plugin URL; Jellyfin `/Sessions` shows the device with `PlayMethod=DirectPlay` and `PositionTicks` advancing on the ~10 s cadence; pause reflects; stop posts final position; resume point visible server-side afterwards.
* **S1.6 device-profile matrix**: files crafted per codec/HDR case: allowed codec → DirectPlay; codec excluded in settings → transcode (assert `/Sessions` `TranscodingInfo` codec + bitrate); HDR type excluded → transcode; force-remux and force-transcode toggles honored; music plays via universal audio with the configured codec/bitrate.
* **S1.7 native resume**: item with server-side resume → dynamic listing exposes it; selecting shows Kodi's native "Resume from hh:mm:ss" (screenshot); both choices land at the right position (`Player.GetProperties` time).
* **S1.8 transcode context popup**: with 3 bitrates enabled the context item lists exactly those 3; choosing one starts a forced transcode at that bitrate (`/Sessions`); with exactly 1 enabled, no dialog (log shows the bypass) straight to transcode.
* **S1.9 soft-restart loop**: trigger the settings restart button 20× scripted; after each: exactly one new service banner in the log, websocket reconnects, server `/Sessions` shows a single device entry, no thread-leak warnings.
* **[PASS] S1.12 toast icons**: an informational toast carries the addon icon, a failure carries Kodi's glyph. Run 2026-07-26 on Omega 21.3 — "Connection OK — minipie (10.11.11)" (plugin process, Test connection) and "Connected to minipie" (service process, websocket open) both render the Kofin logo; a failed resolve ("Server unreachable", `mode=play` with a bogus id) renders Kodi's red error glyph. The mechanism is that `Dialog::notification` treats any icon string that is not "info"/"warning"/"error" as an image path, and an empty one as "info" (`interfaces/legacy/Dialog.cpp`) — which is how two error toasts previously showed the info glyph *and* beeped, having passed neither argument. The warning glyph is L1-only: every live trigger for it (connection loss, a second concurrent sync, a Quick Connect timeout) means breaking something real on the box.
* **[PASS] S1.11 no duplicate context entries in addon listings**: an item's context menu names each action once, and every server-side action sits in Jellyfin actions. Run 2026-07-26 on Omega 21.3 — top level is Kodi's own Play / Play next / Queue item / Information / Mark as watched / Add to favourites / Play from here, then Jellyfin actions and Play with transcoding; Jellyfin actions holds **Mark as watched on Jellyfin** / Add to favourites / Settings (+ Delete when opted in). Before: the listing contributed a watched toggle wearing Kodi's own 16103/16104 wording and a favourite toggle wearing Kodi's 14076, so the menu offered "Mark as watched" and "Add to favourites" twice each, meaning different things. Placement is not the addon's to pick — `CGUIMediaWindow::OnPopupMenu` builds plugin listitem items, then the global menu, then window buttons, then addon extensions, so a listing's own entries are pinned above Kodi's Play and nothing can sit beside a specific native entry. Kodi's own watched toggle stays, and is the *wrong* one for these items: `CVideoDatabase::SetPlayCount` writes a play count against the listing's plugin URL but only announces `OnUpdate` for items carrying a dbid, so it never reaches Jellyfin and never shows up in a kofin listing. Two gotchas worth remembering: the new string ids rendered blank until a full Kodi restart, and a re-navigation to the same plugin path served Kodi's **cached** listing (old context items and all) until `Container.Refresh`.
* **[PASS] S1.10 info dialog in addon listings**: pressing Info (and the context menu's Information entry) on an item in kofin's own plugin listings opens Kodi's video info dialog; library listings keep theirs. Run 2026-07-26 on Omega 21.3 — movie and episode listing items open `movieinformation` (window 12003) with plot/rating/genre/studio/premiered/streamdetails; library movie, tvshow and episode all still open with their full records (Extended info, Trailer, Same director). Both entry points did nothing at all before the fix, with **no log line** of any kind: `plugin://plugin.video.kofin/` carried the tvshows content/scraper pair (the fork's "hack to allow cast information in add-on mode"), a listing item's directory *is* that root, so `OnItemInfo` resolved tvshows content "found directly" and returned early on Kodi's "dont lookup on root tvshow folder" branch. The pair now lives on each library's own path, where Movies already kept it. Not covered: **cast is still absent** ("Cast not available") because `BROWSE_FIELDS` does not request `People` — a payload trade on every listing, not a defect in the dialog.

### Phase 2 gate — library sync

Run 2026-07-17 on the `kofin-test` profile (separate DBs, Omega 21.3) against `jelly.konell.xyz` (Jellyfin 10.11.11, official KodiSyncQueue active) as the non-admin `kofin-test` user. Libraries synced: Movies (1758), Documentaries (tvshows), Music-Alt (1147 songs), Collections boxsets. Evidence per scenario in `tests/live/results/S2.*.md` (local, gitignored). Three bugs were found and fixed in the run, all committed: `/Library/MediaFolders` 403 for a non-admin user (took the view table down — now best-effort, falls back to the user's own views); the widget-refresh policy (see S2.9); and a startup-transient in the settings diff engine that once *prompted a library removal on a plain Kodi restart* (now gated behind a readiness settle + re-baseline). 9 of 11 gates pass (S2.1–S2.9); S2.10 and S2.11 are partial (Update/Repair buttons and the tier-2 status line confirmed; the hole-heal/tombstone assertions and the tier-1/tier-3 transitions depend on later phases).

* **[PASS] S2.1 initial full sync**: per-type counts in Kodi DB equal server counts for the test libraries; scripted field-fidelity diff for one movie and one episode (title, plot, people, streams, art URLs, dates, uniqueids). — 1758 movies / 54 sets / docs / 1147 songs in 1:35; counts match bar two intended diffs (a server-duplicated album merged by MBID; artist rows a superset of top-level MusicArtist items). Movie + episode field fidelity exact.
* **[PASS] S2.2 A/B equivalence (the transplant proof)**: sync the same test libraries with the old fork addon on a scratch Kodi profile, then with kofin; normalized dumps of the video tables (paths normalized for the differing addon ids) must match. Differences must be explainable and intended. — Harness `tests/live/ab_diff.py`; compared kofin's Movies against the master profile's old-addon DB (existing, not a scratch profile). 15 of 36,918 field-comparisons differ (0.041%), all explained: one movie the server replaced with a 4K encode since the master sync (old item 404s), and studio-name casing from Kodi's shared `COLLATE NOCASE` studio table being sync-order dependent (identical fork behavior). TV/boxsets not comparable on this box (master synced all TV libs + only 2 sets).
* **[PASS] S2.3 incremental round trips**: via the Jellyfin API — add an episode file + refresh, edit a title, delete an item, flip watched, set a resume position, favorite — first with Kodi live (websocket path, assert applied within seconds), then repeated with the service bounced (catch-up path). Kodi DB state asserted for each. — watched/resume/favorite/title all applied within seconds over the websocket, and every revert applied on the cold catch-up. Delete covered by S2.8. ("add an episode file" not exercised — needs real media on the server.)
* **[PASS] S2.4 userdata is zero-download**: flip 50 watched states server-side while the service is down; on catch-up, playcounts land with **zero** `Items?Ids=` fetches in the addon log. — 49 flips applied purely from the sync-queue payload; 0 download workers, 0 `Items?Ids=` fetches. Confirmed the `filter` param is an exclude list (kofin sends it correctly) and observed watermark honesty (an empty catch-up that raced the plugin flush did not advance the watermark).
* **[PASS] S2.5 Etag no-op class**: force N re-saves without changes; official-plugin tier: N downloads, zero writes (skip lines); Kofin tier (phase 5): zero downloads too (request-count grep is the assertion). — the Update button re-fetched all synced items (3067 records) in 0:41 with **3067 "Skipping unchanged" and 0 metadata writes**; the movie table md5 was byte-identical before/after. Tier-2 (download-then-skip) proven; tier-1 (skip-before-download) is phase 5.
* **[PASS] S2.6 watermark honesty**: make the server unreachable mid-catch-up; watermark must not advance, service degrades with a notification (never a modal) and retries with backoff; on recovery the missed window replays (marker item arrives exactly once). — server made unreachable to the addon (repointed `serverAddress` to a closed port, sudo-free) with a marker change flipped during the outage: watermark **held** at 21:39:13Z, backoff doubled 10s→20s→40s, no modal; on recovery the catch-up re-queried from the held watermark and applied the missed change **exactly once** (`userdata:1/1`), advancing the watermark only after. One sub-assertion — the `schedule_retry` *notification* — fires on a catch-up failure while *online*; the connect-stage simulation logs+retries quietly by design (fork parity), so that path stays L1-covered (`test_schedule_retry_backs_off`). Evidence: `tests/live/results/S2.6-watermark-honesty.md`.
* **[PASS] S2.7 interrupted full sync**: bounce the service mid-initial-sync; it resumes unprompted and final counts equal S2.1 (page-hole regression). — a fresh initial sync of the Shows library (76 series / 4470 real episodes) was killed mid-sync at 17 shows / 680 episodes; the interrupted `sync.json` held pending `Libraries` + a RestorePoint with Shows not yet whitelisted. On re-enable it resumed unprompted ("Resuming interrupted sync of 1 libraries", a notification not a modal) from the restore point and reached exactly the server counts (76 series / 4470 episodes; whole DB consistent — 1758 movies, 80 tvshows, 4485 episodes). No page holes. Evidence: `tests/live/results/S2.7-interrupted-full-sync.md`.
* **[PASS] S2.8 deletion integrity**: delete a whole series server-side; after sync, zero orphan seasons/episodes/files/link rows (sqlite orphan queries). — Conor deleted the Planet Oil series server-side; kofin removed it in realtime over the websocket, leaving **zero orphans across 23 link-table rules** and no residual kofin.db refs. Permanently removes the media from the server (owner's deletion).
* **[PASS] S2.9 widget policy** (*policy revised this run*): after direct writes, `UpdateLibrary(video)` **is** called — it is a no-op scan (writers stamp `noUpdate=1` on every path; `CVideoDatabase::GetPaths` skips those), measured 3–65 ms with zero "Process directory" lines, and it is the only thing that clears Kodi's cached `Library.HasContent`. `UpdateLibrary(music)` is **never** called (its paths lack noUpdate → 21k remote probes, the e4f8dc3f crash). A one-shot `ReloadSkin()` fires on the empty→non-empty transition to rebuild Estuary's `videodb://` home widgets, self-limiting to the first sync. `Container.Refresh` still only when `Window.IsMedia`. Evidence: `tests/live/results/S2.9-widget-policy.md`.
* **[PARTIAL] S2.10 update/repair buttons**: delete a Kodi-side row manually (hole) and add+remove items server-side beyond retention; *Update* heals both directions via the prune pass; *Repair* rebuilds the library and watched state survives (server tombstones). — the **Update** button works (S2.5: full re-fetch + Etag-skip) and the **Repair** picker/dispatch works (driven live earlier); the specific assertions — a manually-injected Kodi-side hole healed by the prune pass, and watched-state surviving a Repair via server tombstones — are not yet formally run.
* **[PARTIAL] S2.11 tier ladder**: with KofinSyncQueue → status line reads tier 1; plugin disabled → tier 2 against official KodiSyncQueue; both absent (phase 6) → tier 3; each transition logged and the Sync tab status line matches (screenshot). — tier-2 confirmed: the status line reads "Official KodiSyncQueue companion" and `server_time()` is the working tier probe. tier-1 needs KofinSyncQueue (phase 5); tier-3 needs the plugin-free fallback (phase 6). The tier-2 → tier-3 transition (disable the official plugin → status line updates) is runnable now and outstanding.

* **[PASS] S-newcontent new-content notifications**: with `notifyNewContent` on and the whitelist synced, add one unwatched film server-side → exactly one toast naming it; add three → one toast, "3 movies added to library"; add a film already marked watched → nothing; add two episodes of one existing show → "2 episodes of X added to library", and episodes across two shows → the bare count; add a whole new show → the show line only, no episode line behind it; add an album → the album line and nothing per track. Then the gates: toggle the setting off and repeat the first case → nothing; with `syncDuringPlay` on, add a film during playback → nothing until playback stops, one toast shortly after. Selecting a *new* library must stay silent throughout (full sync writes direct, does not announce). — Run 2026-08-03 on Omega 21.3 against Jellyfin 10.11.11 (tier 1): 10 of 12 sub-cases pass live, every message form as specified, the watched movie silent inside a batch of four, the new show's 5 episodes suppressed behind its show line, the album's 5 songs silent, the held toast landing 1.3 s after `Player.Stop`, and a Repair (remove + full re-sync) announcing nothing. Additions were induced through the prune path — items removed via kofin's own removal writers, then healed by **Update libraries** — because no media can be added to this server (API access only); the change feed's own `ItemsAdded` classification is upstream and covered by S2.3. **Artist and music-video lines are not reachable here**: `PRUNE_SERVER_TYPES["music"]` is `MusicAlbum,Audio`, so a removed artist returns as a side effect of its album's write rather than as an addition (the run produced the album line, correctly), and no music-video library is synced on this box — both stay L1-covered. Long messages *scroll* in Estuary's notification rather than truncate. Evidence: `tests/live/results/S-newcontent-notifications.md`.

### Phase 3 gate — player features

Run 2026-07-18 on the `kofin-test` profile (Omega 21.3) against `jelly.konell.xyz`. Test media: the Shows library (server MediaSegments provider serving Intro/Outro/Recap), **The 25th Hour** (2 movie extras), **Better Call Saul** and other shows (TV extras). All 5 gates pass; six bugs were found live and fixed, all L0/L1-covered — evidence per scenario in `tests/live/results/S3.*.md` (local, gitignored): (1) the addon skin dropped `media/white.png` so the overlay rendered text-only and the focused button was invisible; (2) the overlay's Python callbacks never fired because it is created on the `SegmentChecker` thread, which waited on `threading.Event.wait` (never yields to Kodi) — now `monitor.waitForAbort`; (3) a t≈0 auto-skip seek was dropped and falsely toasted — now deferred toast + seek retry; (4) select routed to the focused button in `onAction`; (5) Play Next resumed the next episode instead of starting it fresh — now `fromstart=1`; (6) a stale pre-switch `getTime()` fired a phantom Skip Outro on the Play-Next'd episode — now gated on the item's intended start.

* **[PASS] S3.1 segment auto-skip**: seek to shortly before a known intro (`Player.Seek`); position jumps past segment end within the 0.25 s poll interval; notification shown; EOF-clamped segment never seeks past runtime; an Intro at t≈0 still fires (warm-fetch gate); a fast seek that steps *over* a segment still auto-skips (boundary-crossing). — Ted Lasso intro 198.9→219.2 (jumped in one poll gap), "Skipped Introduction" toast screenshot; Simpsons t≈0 intro 0→88.4 after the seek-retry fix; EOF-clamp/crossing L1.
* **[PASS] S3.2 skip button**: button mode shows the overlay at the segment crossing (screenshot); pressing Skip seeks to segment end; ignoring it auto-closes past segment end; never two overlays at once. — Skip Recap/Skip Intro overlay renders (after the white.png fix) and OK skips (after the waitForAbort fix), confirmed live with a remote; auto-close + "never two" verified (L1 for the latter).
* **[PASS] S3.3 unified Play Next dialog** (kofin-owned, no service.upnext installed): an episode with a Credits segment and a next episode shows **one** kofin overlay at the credits crossing with **Skip Outro / Play Next / Close** (screenshot); Play Next starts the next episode through kofin; Skip Outro seeks past credits; the season's last episode shows **Skip Outro / Close** only; a show with **no** Credits segment shows the **near-end Play Next prompt** at `end − playNextLeadTime`; inside a SyncPlay group, Play Next is withheld. Never two overlays at once. — all three buttons live; Play Next starts the next episode from the beginning (after the fromstart + fresh-start fixes); finale/near-end/SyncPlay variants L1.
* **[PASS] S3.4 movie extras**: after sync, the two-extras movie has `videoversion` rows (itemType per schema) with sensible names; the info dialog shows the Extras button (screenshot); an extra plays and reports. — The 25th Hour writes 2 `itemType=1` rows ("Behind the Scenes"/"Extra"); extras viewable + play live. (Duration fix in #25: streamdetails on each extra's file so the UI shows feature runtime, not the film's.)
* **[PASS] S3.5 TV extras**: the show with specials exposes the Extras browse node; entries play. — browsable live; listing builder L1.
* **[ ] S-versions multi-version movies**: after sync, a multi-MediaSource movie has VERSION `videoversion` rows (primary + alternates); info dialog Versions UI lists edition names from `MediaSource.Name`; each version plays the matching MediaSource (`mediasourceid` in the play URL / log); primary resume/watched still works. L2 covers Omega+Piers writers; live on Omega when test media is available.
* **[PASS] S3.6 Jellyfin song lyrics**: with the visualisation window open (script.cu.lrclyrics only searches there), play a song the server has lyrics for and assert `Window(Home).Property(culrc.source) == 'Jellyfin'` — one assertion covering the whole chain, since the property is set by the lyrics addon only once it has accepted our text. Repeat for a direct-path row and a `plugin://` (`musicTranscode`) row; a song with no server lyrics must leave `MusicPlayer.Lyrics` empty and let the addon scrape; video playback must log no lyrics attempt. — Run 2026-07-27 on Omega 21.3 against Jellyfin 10.11.11: `Source: Jellyfin` rendered with correct synced-line highlighting (screenshot), both path shapes pass, the no-lyrics song fell through to the addon's `genius` scraper, and a movie produced no attempt and no errors.

**The race is the whole feature on the hand-off path, and it is one-shot.** lrclyrics searches on `onAVStarted` and **memoises the result per song**; its own force-refresh does not clear that memo, so lyrics arriving even ~200 ms late are not merely late — they are ignored until the song falls out of its 10-entry cache. A first spike that pushed after AV start lost by 190 ms and could never recover the track. Pushing from `onPlayBackStarted`, *above* the claim, wins with room: the whole chain (resolve id → kofin.db lookup → HTTPS fetch → push) measured 89 ms warm and 227 ms cold, against a gap to `onAVStarted` that is however long opening a network stream takes. Any future work that adds latency ahead of `Player.start_lyrics` — or moves it below `_claim()`, which blocks for seconds — silently breaks this without failing a unit test. The live assertion above is the only thing that catches it.

**Testing the hand-off needs lrclyrics' memo cleared, or the run is meaningless.** A song it has already searched returns the cached scrape without ever reading `MusicPlayer.Lyrics`, so a correct implementation looks broken. This cost a debugging cycle on 2026-07-28: a re-run showed `culrc.source=musixmatchlrc` while the log proved kofin's push had landed a full 2 s ahead of lrclyrics opening its window. Bounce `script.cu.lrclyrics` (disable → enable) before each hand-off run, or use a song it has never seen. Same discipline as the S5.3 `since`-window trap: an unfair comparison produces a confident, wrong conclusion.

* **[PASS] S3.7 skin lyrics overlay** (`musicLyricsMode=1`): playing a song with timed lyrics publishes the ladder on window 10000 — `kofin.lyric.has=true`, `kofin.lyric.active=true`, and `kofin.lyric.<LYRIC_SLOTS//2>` holding the line the clock is on; the ladder advances as the song plays; stopping clears every slot. Untimed lyrics publish with `active` empty. Skin mode must not also drive a lyrics addon, and vice versa. — Run 2026-07-28 on Omega 21.3: centre slot cross-checked against the server's own timings at t=49.1 s (`'As I walk this world alone'`, exact match), advance observed at t+7/+20/+33 s, teardown leaves all nine slots empty, and `musicLyricsMode=2` leaves `kofin.lyric.has` unset while the tag push still lands.

**Why the overlay lives in the skin and not in an addon window.** A Python `WindowXMLDialog` shown with `show()` becomes the *active* window (`currentwindow` flips to the addon's id), and `CGUIWindowManager::HandleAction` then routes every action to it. Measured 2026-07-28 with a throwaway addon carrying **no focusable controls at all**: `pause`/`play` still reached the player (they fall back to global handling), but `osd` (24), `back` (92) and all navigation were swallowed. Blocking the OSD is disqualifying on its own — it is where a skin's lyrics button lives. Removing focusable controls does not help; the problem is window activation, not focus. A second hazard, seen the same day: when lrclyrics was force-killed mid-test its window stayed registered and **pinned the UI to that window id** — `GUI.ActivateWindow`, `Input.Back` and `RestartApp` all failed and it took a manual restart to clear. An always-on addon window inherits that failure mode. Skin controls belong to the window that is already active, so they cost neither.

### Phase 4 gate — SyncPlay

The scripted second group member is `ref/syncplay-conformance` (`syncplay_kit` — join/command/observe), run against the `kofin-test` profile. Assertion surfaces in trust order: Jellyfin `/SyncPlay/List` + the harness's own logged timestamps; Kodi JSON-RPC `Player.GetProperties` (`time`, `speed`, `canchangetempo`); the addon debug log via `kodi-logtail`; screenshots last (pixels prove rendering, never state).

Run 2026-07-19 on the `kofin-test` profile (Omega 21.3; a fresh Kodi restart was needed once to load the phase-4 addon PO — new addon strings are cached until restart) against `jelly.konell.xyz` (Jellyfin 10.11.11), with a controllable host built on the kit's reference client (creates/hosts a group, sets a queue, reports Ready/Pause/Seek/Unpause/Buffering on command, logs every WS event). **5 of 7 gates fully pass** (S4.1, S4.2, S4.4, S4.5, S4.6); S4.3 is partial (Omega cannot demonstrate the smooth `SetTempo` path — a Kodi bug, fixed in Piers) and S4.7 is the pending manual two-device pass. Masking clean, no regression when SyncPlay is idle, and `syncPlayEnabled=off` builds no manager and no root entry — all verified. Evidence per scenario in `tests/live/results/S4.*.md` (local, gitignored). `canchangetempo` is not a JSON-RPC `Player` property in Omega; tempo availability is read from the `videoplayer.usedisplayasclock` setting instead.

* **[PASS] S4.1 join + follow (K1)**: join a harness-hosted group; a scheduled Unpause executes within `syncPlayTolerance` of `When` (Kodi `Player` time vs the harness's `When`, offset-corrected); Ready is posted with the correct position; harness-driven Pause/Seek propagate to Kodi (follow-only, before K2 forwarding exists). Never a second player start on a tail-only queue update. — join menu renders ("SyncPlay", group + `[Waiting]` state); the group-start `Unpause` scheduled `+996ms` and the player went 0→1× at the target instant (within one 120 ms poll sample of `When − offset`), then tracked wall-clock; Ready held at pos ≈0; harness Pause `(-3ms)` and Seek (46→120 s) propagated; exactly **one** player start across join→start→pause→seek→resume.
* **[PASS] S4.2 user-control forwarding (K2)**: pause/seek/next in Kodi drive the group (harness observes the echoed authoritative command); the guard flag prevents an echo loop (no runaway command storm in the log); a Kodi stop offers leave-or-ignore. — user pause/resume/seek each drove **exactly one** authoritative command (`[ syncplay/user pause|unpause|seek ]`; guard held, no storm); a Kodi stop raised the leave-or-ignore dialog (*Stop for everyone / Become a spectator / Leave group*).
* **[PARTIAL] S4.3 drift correction**: induce drift (harness advances/holds); `SetTempo` correction observed via `Player.GetProperties` `speed` moving off 1.0 within the clamp, then re-converging to the dead-zone; with `canchangetempo=false` (passthrough) the micro-seek ladder holds instead; `syncPlayDriftCorrection=off` tolerates within the dead-zone and never tempo-corrects. — live on Omega: tempo feature-detect (False when `usedisplayasclock` off → tolerate, True when on), the tolerate branch (speed held 1.0, no tempo/seek), and gross-drift tolerate (>1500 ms). The active `SetTempo` engage/re-converge could **not** be shown because **Omega's tempo→1.0x restore skips** (Kodi bug, fixed in Piers) — kofin's `_verify_tempo_restore` disables tempo on detection; the passthrough fallback is *tolerate + server re-sync*, not a micro-seek ladder (the drift loop never seeks). The engage matrix + the Omega restore-skip→disable are L1-verified (19 units); smooth tempo is a Piers runtime pass.
* **[PASS] S4.4 timesync**: offset stabilizes (min-RTT window); `forceUpdate()` on join produces a fresh offset before the first Ready; a screensaver-deactivate re-syncs (log shows the forced update). — the window converges the offset onto the lowest-RTT sample; join's greedy pings land offsets before the first Ready; a screensaver-deactivate logged `screensaver deactivated` and **reset the window to 1** (`force_update(reset=True)`) — the same wake also ran the kicked-probe re-join.
* **[PASS] S4.5 resilience**: harness drop-out surfaces the group-wait toast; a WS drop → on reconnect the manager probes `/SyncPlay/List` and auto-rejoins the previous group; a late command (`When` in the past) executes-now and extrapolates the live position; sleep/wake leaves and rejoins cleanly. — a member `Buffering` report put the group `Waiting (Buffer)` and raised the **"Waiting for the group…"** toast; late commands executed-now (`-1984ms`); the kicked-probe re-join is live (via the wake path, S4.4) and L1 (9 units); sleep/wake re-syncs and re-probes. A literal transient socket drop needs root-level net control; the rejoin mechanism it invokes is live-proven.
* **[PASS] S4.6 interactions**: inside a group the near-end Play Next prompt is withheld (the phase-3 `syncplay_group_active` flag — assert no overlay, no phantom Play Next), cinema/up-next suppressed; selecting an external player hides the SyncPlay root entry. — with the next episode **resolved** in the group, the near-end Play Next log+overlay fired **out of** a group and was **withheld inside** one (no `near-end Play Next prompt` log, no overlay) — the `_play_next_on_offer` gate; an installed `playercorefactory.xml` hid the SyncPlay root entry (restored on removal); cinema/up-next N/A (upnext not installed, cinema unbuilt).
* **[PARTIAL] S4.7 manual two-device sanity**: this box + a second client (jellyfin-web or a second Kodi) in one real group — join, play, pause/seek both directions, one drift re-converge, leave — as the final judgment pass. — the scripted second member exercised the full two-member protocol across S4.1–S4.6; the human soak with a real rendering peer (web+Kodi cross-implementation) is the designated **manual** pass, left ready (`syncPlayEnabled` on, Kodi debug logging on).

L1 units port from the fork's `tests/test_syncplay*.py` (~1.8k lines): command scheduling incl. late-`When` extrapolation, the drift-controller branch matrix, the timesync min-RTT window, the queue-mirror tail-diff, and echo-guard suppression. New kofin units: the play-path re-target (a group play resolves through `plugin_url(mode=play)`, not a raw path), the kicked-probe decision, and `syncPlayEnabled` gating the root entry. Also: a masking grep over a full SyncPlay session (tokens/user-id/device-id in `/SyncPlay/*` and WS traffic never survive the log chokepoint), and a quick S3.1/S3.3 re-run showing segment skip + Play Next unchanged when SyncPlay is idle.

### Phase 5 gate — KofinSyncQueue

Plugin xunit (`jellyfin-plugin-kofinsyncqueue`): **29 pass** — coalescing matrix, include-list parsing, retention envelope math, per-user userdata isolation, and Etag population incl. the removed-record null. The Etag leg had no coverage until 2026-07-20; testing it required extracting the controller's record projection into a BaseItem-free seam (`Api/RecordProjection.cs`) so the Etag and visibility rules are unit-testable without mocking `ILibraryManager` — the same seam shape `ItemEvent` already uses on the write side.

Run 2026-07-20 on the `kofin-test` profile (Omega 21.3) against `jelly.konell.xyz` (Jellyfin 10.11.11), KofinSyncQueue 1.0.0.0 deployed alongside the official Kodi Sync Queue 15.0.0.0. The ladder, both recorders, coalescing and **Etag equivalence** are green; the remaining tier-1 scenarios are unblocked but not yet run. Evidence in `tests/live/results/S5.*.md` (local, gitignored).

* **[PASS] S5.1 tier ladder + status**: tier-1 detection (`KofinSyncQueue companion detected (plugin 1.0.0.0, protocol 1)`), confirmed server-side by the plugin's own `SyncQueue since ...` log; the 404 fallback to tier 2 and the both-tiers-503 degrade were both observed live. Tier-1 selection logged nothing until this run — the detection line was added to make the gate assertable. Tier-3 fake-protocol leg and the S2.11 tier-2 → tier-3 transition still outstanding.
* **[PASS] S5.2 typed-feed ordering (additions leg)**: a real show — *I Will Find You (2026)*, 1 season / 8 episodes — moved into the Shows library with the service **down**, then scanned. The feed returned 10 `Added` records carrying `SeriesId`/`SeasonId` on every child (the typed enrichment tier 2 cannot supply), for a series kofin.db had never seen. The whole batch went out as **one** `Items?Ids=` request whose id order decodes to Series → Season → Episodes 1–8 — parent-first by construction, with the parent prefetched as its own addition rather than discovered by failure, so the R8 orphan re-queue never engaged. Per-class progress shown (`---[ added:10 ]`). Result: `tvshow` 81→82, `episode` 4511→4519, zero orphans. Not covered: metadata + image edits in the *same* mixed batch, image-only sorting behind metadata (proven separately in S5.5), and removals-before-additions.
* **[PASS] S5.3 step-1 smoke: event capture, coalescing, Etag equivalence**: both recorders subscribe and fire (`Kofin library change recorder subscribed` / `... user-data recorder subscribed`, log lines added this run). A favourite toggle and its revert produce **one** user-data row, not two; creating a collection logs `Recorded 4 library change events` that coalesce to **one** `Added BoxSet/boxsets` record; deleting it coalesces Added+Removed → a single `Removed` record with no Etag. The load-bearing check passes: the record's query-time Etag `bdd6f31956f9e347d222be28dda507f4` is **byte-identical** to the DTO Etag for the same item and user. The `UpdateReason: ImageUpdate` leg is not yet exercised. Two false alarms are written up in the results file — `ItemUpdated` does **not** fire for `POST /Items/{id}` on 10.11.11 (the official plugin misses it too, so a metadata edit is the wrong lever for the library recorder — use item add/remove), and a `since` captured in the same wall-clock second as the event correctly returns nothing because the protocol compares strictly.
* **[PASS] S5.4 userdata zero-download, tier 1**: 49 watched flips applied while the service was down came back as `userdata:49/49` in one batch with **zero** item downloads, 49 of 49 verified in the Kodi DB. The watermark advanced to `11:29:26Z` — exactly the envelope's `ServerTime`, with **no** `−2 min` fudge (tier 2 would have stamped `11:27:26Z`), which is the tier-1 watermark rule proven directly.
* **[PASS] S5.3 skip-before-download**: 10 collections synced, then the watermark rewound so the same 10 records were re-delivered with Etags kofin already held — `---[ skipped unchanged:10 ]`, **0** `Items?Ids=` requests and **0** downloads, against 1 batched fetch and 2 downloads on first delivery. Dropped *before* download, which is the phase-5 thesis; on tier 2 all 10 would be fetched and then discarded. Measurable only because `core/http.py` now logs every request at debug level (masked as usual) — no request-count assertion in this plan was previously possible. Incidental: the feed query sends `types=tvshows,movies,music,music,tvshows,boxsets`, an un-deduplicated include list (harmless, now visible).
* **[PASS] S5.5 image-only demotion**: a backdrop uploaded while the service was down produced `Kofin event: Updated "Movie" … reason=ImageUpdate` and a feed record carrying `UpdateReason: 'ImageUpdate'`. On catch-up the record routed to the **artwork** queue, not the general update queue (`---[ artwork:1 ]` → `q:download/artwork/…`), fetched with **minimal fields** (`Items?Ids=…&Fields=Etag`), and applied through `artwork_only` (`Artwork-only update movie …`) with **no metadata cascade** — the only `kodidb.movies` lines in the window are the startup Omega migration. Kodi's `art` rows for the movie gained the new Backdrop URL. Not exercised: a combined `ImageUpdate|MetadataEdit` reason taking the full path.
* **[PASS] S5.6 retention overrun (tier 1)**: retention set to 1 day and the watermark forced 2 days back produced `sync queue retention exceeded (watermark … < cutoff …); scheduling a library update` — one notification, then the targeted update pass ran **automatically** over all six libraries, with the watermark advancing only afterwards (to the pass's start time, so changes during the pass are not skipped). R5's silent loss becomes self-healing. Retention restored to 90.
* **[PASS] S5.7 plugin load check**: the gate's "timed from each plugin's own log" comparison is **unobtainable** — the official KodiSyncQueue emits no write diagnostics at any level (22 log lines all day: load, start, dispose, DI), and raising the server to Debug did not change that. Substituted an equivalent measured identically for both: after a 200-collection delete burst (15.9 s, 6-way parallel), time until each plugin's API serves all 200 removals — **Kofin 22.0 s vs official 40.1 s** (≈6 s vs ≈24 s measured from the end of the burst). Direction matches the indexed-upsert-vs-`O(n²)` expectation, but one run at n=200 on a live server is an indication, not a benchmark, and the figure includes each plugin's batching interval rather than isolating write duration. Stronger result from Kofin's own log: each storm absorbed into **one** batch (`Recorded 800 library change events` for 200 creates, `Recorded 200` for the deletes), and the client saw 400 events **coalesced to 200 `Removed` records** in a single cycle. Lever caveat: a collection burst exercises add/remove and coalescing, not the metadata re-save path the plan describes.

**Per-tier catch-up latency (§5 baseline) — done.** Identical lever on both tiers, zero server mutations: rewind the watermark over records already in both queues and time the drain to the `--[ sync/<ISO> ]` stamp. Same 30-minute window, same 100 mixed changes (`userdata:50/50` + `removed:50`): **tier 1 = 4.1 s / 9 requests / 0 item fetches; tier 2 = 6.1 s / 11 requests / 0 item fetches.** Tier 2 verified genuine (`no KofinSyncQueue companion detected … -> 404`, then `GetServerDateTime` + legacy `GetItems`), with the Kofin plugin disabled server-side for the run. The gap is small **and should be** — removals and user-data are zero-download on both tiers, so nothing is left for the Etag skip to save; the two extra requests are tier 2's `GetServerDateTime` round trips, which tier 1 avoids because the envelope carries `ServerTime`. The tier-1 win is on the Added/Updated path, measured in S5.3. Two earlier attempts were discarded rather than reported: a timer that returned its own 600 s deadline, and a mutation lever whose first catch-up preceded the plugin's batch flush and therefore measured kofin's poll interval rather than the tier.

**A "finding" that was a testing error — recorded because the mistake is the lesson.** This plan briefly asserted that KofinSyncQueue never records `ItemUpdated`, on the evidence that a backdrop upload appeared in the official plugin's feed and not in ours. **That claim was false.** Instrumenting the recorder showed the event arriving normally (`Kofin event: Updated "Movie" … reason=ImageUpdate` → `Recorded 1 library change events`). The real cause was the query, twice over: the record's `LastModified` was *equal* to the `since` value taken from a local clock read in the same wall-clock second, and the protocol compares **strictly** (`>`), so our feed correctly returned nothing — while the cross-check against the official plugin used a window 60 s wider, so it returned the item. An unfair comparison produced a confident, wrong conclusion about a plugin defect.

The same second-resolution trap had already been hit and written up once in `S5.3-recorder-smoke.md`, then walked into again. Standing rule for every live scenario from here: **`since` comes from the previous response's `ServerTime`, never from a local clock read taken around the mutation**, and any two feeds being compared must be queried over the *identical* window.

**Third live finding: BoxSet removals never applied.** `RemovedWorker`'s dispatch had no `BoxSet` branch and no `else`, and `obj` was assigned inside the drain loop — so a BoxSet removal raised `UnboundLocalError` and left the rows in place, or (after a Movie removal in the same drain) silently reused `movies.remove` on a set id. Tier 1 surfaced it because the change feed delivers the same nine kinds the addon syncs. Dispatch is now a pure `removal_writer_for()` returning the writer per item — `Movies.remove` dispatches on the mapping row's media, so a boxset id correctly removes the set — with L1 covering every synced kind, the unknown-kind case, and the unbuilt writer family (a worker builds video *or* music writers, never both). Verified live: BoxSet rows 66 → 56, Kodi `sets` 66 → 56.
* **[PASS] S5.8 full-sync overhaul**: a full Shows repair (77 series / 4504 episodes) in **103 s**, 187 requests. **Newest-first proven exactly, not inferred** — the repair re-added every show, so `idShow` autoincrement order *is* write order, and it matches the server's `DateCreated,SortName` descending order in **77/77 positions**. **Zero** per-show `/Shows/{id}/Episodes` calls (one per show before phase 5), measurable only because `core/http.py` now logs every request. **S2.7 kill/resume**: killed at episode 216/4504, restart logged `Resuming interrupted sync of 1 libraries` and finished in 85 s with counts exactly 82 / 4504 and zero orphans. Restore points are *not* durable across an abrupt stop (`set_restore_point` mutates the in-memory dict without `save_sync`), so a resume restarts the library's passes rather than continuing inside one — pre-existing transplant behaviour, identical before phase 5, and harmless because the writes are idempotent. **Finding: the per-show request storm moved rather than vanished** — `/Shows/{id}/Seasons` fired **77 times**, once per show, because the writer's season-on-demand call runs for every show instead of acting as the rare fallback the plan assumed. So "a 500-show library costs pages, not 500+ requests" is only half achieved; closing it means the writer consuming the paged seasons, which is transplant territory and a design decision, not a patch.
* **[UX] progress dialog**: the 3-pass tvshows walk runs a fresh progress cycle per pass, so a library sync visibly goes 0→100% three times. The plan intended a per-class label ("New: 12 | Updates: 340") instead. Separately, killing the addon mid-sync leaks the background progress bar — `progress()` closes it in a `finally`, but disabling the addon kills the Python thread outright, so the bar stays on screen (stuck at e.g. "Shows 5%") until Kodi restarts. Reachable in normal use, since disabling the addon is a supported action.
* **[PASS] S5.9 ids+Etag Update**: the Update over all six libraries took **81 s** with zero errors and exercised all four planner legs. **match → nothing**: the two music libraries (22034 songs) diffed `0/0/0` with **no item downloads at all** — the S2.5 cost (3067 re-fetches just to read Etags) is gone. **stale → removed**: 21 spurious movie rows were pruned, taking `movie` from 1789 back to **1768**, the session-start baseline — the prune self-healed the regression's corruption. **changed → fetched** (330+2+1) and **missing → fetched** (12+3) went through the incremental pipeline, not a full walk. **Watermark half**: a watched flip made *during* a running full sync was replayed by the catch-up after it (`---[ userdata:1/2 ]`), i.e. the post-sync window still covered changes made mid-sync rather than skipping them, and `full_sync.py` carries `stamp_watermark_if_empty()` at start with **no `save_last_sync` anywhere** — the end-stamp is gone as the plan required. Caveat: the websocket also delivered that change while the client was online, so this shows the catch-up window is correct, not that the change would have been lost without the fix; demonstrating the loss would need the realtime path suppressed. `add_library` of a second library while changes for the first are pending is still unexercised.

**Phase-5 regression found live and fixed — this is what the gate was for.** `downloader.get_items()` accepts an `item_type` argument and, since the phase-5 commit, never put it in the query: `"IncludeItemTypes": item_type` was dropped when the newest-first `SortBy`/`SortOrder` lines were added. The 3-pass tvshows walk therefore issued the same unfiltered query three times and applied the wrong writer to each item — the Series pass ran `obj.tvshow()` over episodes, whose `/Shows/{id}/Seasons` 404s, aborting the library and re-firing a sync-failed toast on every service start. It corrupted the test profile with 21 tvshow rows that were really seasons and episodes. The existing phase-5 tests asserted the *new* sort fields and never that the pre-existing filter survived, which is exactly how it shipped green; `test_item_type_filter_reaches_the_query` now covers it (verified to fail without the fix). A per-item 404 guard added while misreading the symptom made it worse — silent mass-skipping instead of a loud abort — and is kept only with a per-pass skip count so a partial pass cannot look complete. Full write-up in `tests/live/results/S5.8-S5.9-full-sync-and-prune.md`.

Tier-2 neutrality (the step-2 hard gate, run before tier-1 code was exercised): **S2.4 rerun PASS** — `userdata:49/49` in one batch, **zero** item downloads, 49 of 49 verified as `playCount >= 1` in the Kodi DB, reverts round-tripping back through the realtime path (the S2.3 userdata leg). The `LegacyFeed` provider is behaviourally identical to the direct `GetItems` call it replaced.

Two defects found live and fixed, both outside the sync path:

* **Server-down on deploy** (plugin README): Jellyfin rewrites `meta.json` on plugin load and `PluginManager.CreatePluginInstance` does not catch the failure, so a root-owned plugin directory takes the **whole server down** at startup with `UnauthorizedAccessException`. The install docs now make the `chown` mandatory and name the symptom.
* **Library-wipe prompt from a failed settings read** (`service/settings_apply.py`): Kodi logged `failed to load addon settings from ...settings.xml` four minutes into a *ready* session and handed back `""` for `librarySelection`, which the applier read as "user deselected every library" and offered to remove all six. The existing transient guard only covered the pre-`mark_ready()` startup window. Emptying a guarded setting is now corroborated by a second read (`get_str` builds a fresh `Addon()` per call, so the failed instantiation is not repeated); a genuine deselect-all reads empty twice and still proceeds. L1: `test_post_ready_empty_read_is_corroborated_not_obeyed` (verified to fail without the guard) and `test_deliberate_deselect_all_still_removes`.

Also fixed in the plugin: an api-key request to `/Kofin/SyncQueue` returned 400 with a stack trace instead of 401, because an api key carries an all-zeros user claim that `Guid.TryParse` accepts and `GetUserById(Guid.Empty)` then throws.

### Boxsets robustness gate — guard, heal, sweep, drift probe

Run 2026-08-03 on the `kofin-test` profile (Omega 21.3) against `jelly.konell.xyz` (Jellyfin 10.11.11), branch `fix/boxsets-healing` per `docs/boxsets-robustness-plan.md`. **All four gates pass** — evidence in `tests/live/results/S-boxsets.md` (local, gitignored): (1) **post-upgrade migration** — first boot after install, the drift probe flagged all 54 sets (no `boxset_state` rows), the scheduled pass healed all 54 in 11 s (`boxsets: 54 checked (… 54 healed …)`), stamped counts agree with MyVideos exactly, and the next boot is silent; (2) **manufactured drift + ghost** — one member's `idSet`/`parent_id` nulled (the remove/re-add shape) plus a planted set reference absent from the server: one service bounce produced `2 of 55 set(s) unhealthy` → `1 healed, 1 swept`, links and mappings verified restored by SQL; (3) **Refresh boxsets button** end-to-end on the new code (`reset 54 set(s)` → `54 written`, state consistent after); (4) the **unlink guard** is L2-only (a 200-with-zero-items answer for a populated set cannot be manufactured against the real server) with its server-behaviour basis verified live: deleted/bogus ParentId membership queries fail loudly with HTTP 400, and ChildCount is absent unless requested, user-scoped, and counts non-movie children (mixed probe set: ChildCount 2 vs 1 movie member).

### Widget refresh gates — ownership, settle, fingerprint (docs/widget-refresh-plan.md)

Live gates for the refresh rework; run against the `kofin-test` profile with the probe-scan log line (`does not exist - skipping scan`) and `VideoInfoScanner` lines as the counters, kofin's own `widgets moved/unchanged` INFO lines as the gate's self-report.

* **[PASS] W1 quiet wake** (2026-08-04, stack tip): screensaver wake → `screensaver deactivated; catching up` + `retrieve changes`, **zero** scans (pre-rework baseline the same morning: two no-op scans per wake).
* **[PASS] W2 update command** (2026-08-04, full-whitelist variant): `UpdateLibrary` command on a clean tree → `Update pass planned in: 0:01:18`, nothing queued, **zero** refreshes (was plan-time + tail = two per command); the per-library-with-changes variant (exactly one drain-time refresh) still owed a run.
* **[PASS] W3 echo suppression** (2026-08-04): an unfavorite echo ran the full cycle then logged `widgets unchanged: video; refresh suppressed`, zero scans; a genuine watched flip logged `widgets moved: video/userdata` with exactly one scan ~5 s after the drain (the settle), and the flip back likewise.
* **[PENDING] W4 track change**: music playlist next-track → at most one music refresh (two echoes fold into the settle); same-album next-track → zero (order unchanged).
* **[PENDING] W5 removal**: removing a music library → albums leave the music widgets without a restart (music probe fired, not video).
* **[PENDING] W6 first content**: fresh-profile first video sync and first music-only sync → widget sections appear with no manual reload; reload deferred while video plays and fired at stop.
* **[PENDING] W7 container scoping**: browsing a movie listing during a music-only cycle → no `Container.Refresh` on the video window.

### Clean databases gate — the migration cleaner (docs/clean-databases-plan.md)

Run 2026-08-05 on the `jellyfin-kodi` fixture profile (Omega 21.3; 1771 movies / 83 shows / 4502 episodes / 20770 songs synced by jellyfin-kodi 1.6.4, 29664 `jellyfin.db` mappings, 5 view playlists, full node tree), jellyfin-kodi disabled first per the README order; dialogs driven over JSON-RPC. **[PASS]** — evidence in `tests/live/results/cleaner-live/` (local, gitignored): MyVideos left with only `version` and the 387 owner-0 `videoversiontype` seeds; MyMusic left *pristine with seeds* (`[Missing Tag]` artist + `Artist` role re-inserted — the repair jellyfin-kodi's own reset lacks); `jellyfin.db`/`-wal`/`-shm` deleted; the texture purge removed all 93 `/Items/` art rows and their files while keeping the 142 addon/skin rows; every `jellyfin*`/`kofin*` node and playlist entry removed with the copied standard tree and hand-made files untouched (user-nodes toggle declined, the shipped default); the detection-driven music prompt focused Yes with 20k `/Audio/` rows present (screenshot-verified); `RestartApp` respawned Kodi cleanly. The fixture was restored from the pre-reset snapshot afterwards and jellyfin-kodi re-enabled (catch-up sync clean).

### Critic ratings gate — the rating set and its pointer (docs/critic-ratings-plan.md)

Run 2026-08-05 on the `kofin-test` profile (Omega 21.3, 1771 synced films) against `jelly.konell.xyz`, branch `feat/critic-ratings`.

* **[PASS] R1 writer against the real library** — the real movie writer run against *copies* of the live MyVideos131 + kofin.db with real `/Items` payloads: films with a critic rating gained a second row rescaled onto Kodi's scale (`2 Guns` 64 → 6.4, `3 Godfathers` 87 → 8.7) with the pointer left on community; the same payloads re-written changed nothing (the Etag short-circuit — a distinct Etag is needed to exercise the write at all); with the setting on the pointer moved to the critic row, while `The 25th Hour` (no critic rating on the server) kept its community one. Zero dangling `c05`, zero orphan rating rows, over the whole 1771-film set.
* **[PASS] R2 repoint pass at scale** — `repoint_ratings` over all 1771 films, both directions, ~200 ms per pass: sample pointers flipped to critic and back, films without a critic row stayed put, and no rating row was rewritten.
* **[PASS] R3 setting → service → refresh** — the toggle renders on the Library tab with its help text (full Kodi restart first: new `strings.po` ids). Flipping it on logged `setting preferCriticRating changed: 'false' -> 'true'; applying` → `command/RepointRatings` → `ratings repointed to critic over 1771 film(s)` → a video refresh; flipping it back logged the pass again and then `widgets unchanged: video; refresh suppressed` — correct on both counts, because with no critic rows in this library yet the pass genuinely moved nothing. Live database verified intact afterwards (1771/1771 valid pointers).
* **[PENDING] R4 rendered in the UI** — needs a Movies repair to backfill critic rows into an existing library; nothing here has been re-synced since the writer changed, so the flip is a no-op on this profile by construction.

### User-route migration gate — off `/Users/{userId}/…` (2026-08-05)

Prompted by an audit of every route the addon calls against the live server's own OpenAPI document (10.11.11): nine endpoints were absent from the spec while still answering 200, all of them the `/Users/{userId}/…` shape Jellyfin 10.9 replaced.

* **[PASS] U1 equivalence, old vs new** — all six read replacements returned **byte-identical bodies** to the routes they replace. `/UserViews` appeared to differ; the only field was `ChildCount`, which returns different values on three consecutive identical calls *on both routes* (server-side noise kofin does not read). Both mutating pairs (`/UserPlayedItems/{id}`, `/UserFavoriteItems/{id}`) returned 200 for POST and DELETE, with the probe item's state recorded and restored.
* **[PASS] U2 the migrated code against the server** — the real `Api` and `downloader`, not hand-written URLs: `views()` 9 libraries, `items()`/`item()` the same film, `resume()` 18 in progress, `chapters()` 27 chapters on *8½*, `get_item_count()` 1765, `get_items()` a full first page, `get_user_artwork()` on `/UserImage`, and the played/favourite toggles round-tripped with state restored. 13/13.
* **[N/A] extras and trailers** — `special_features()` answers 0 because **no item in the library has extras** (1854 sampled, zero with `SpecialFeatureCount`), so its equivalence rests on U1's identical-body comparison. `get_local_trailers()` was proven on real data: the library's one film with a local trailer (*Poolhall Junkies*) returns it through the new route.

### Stream-menu status gate — what the menu says is playing (2026-08-05)

Run on the `kofin-test` profile (Omega 21.3) against `jelly.konell.xyz`, branch `fix/stream-menu-status`. Menu opened by `RunPlugin(plugin://plugin.video.kofin/?mode=streams)`, which is the same entry point the context item uses.

* **[PASS] M1 a burned-in subtitle is the marked row** — *12 Angry Men* forced-transcoded with `subtitleindex=5&burnsubs=1`: the published payload carries `SubtitleStreamIndex: 5` with that stream at `DeliveryMethod: Encode`, and the subtitle dialog opens with the cursor on **"English - Default - PGSSUB [active]"**. The burned row alone carries no "(burned in, restarts playback)" suffix, the other twenty image tracks all do, and "None" is unmarked. Before the fix the player reported no subtitle at all, so "None" was marked and the row actually on screen looked unselected.
* **[PASS] M2 no second subtitle over a burned-in one** — `subtitleenabled: false` while Kodi still holds the attached SUBRIP track it would otherwise have auto-selected; the burned PGS renders in the picture (screenshot: "a reasonable doubt —"). Log: `subtitle 5 is burned in; Kodi's own left off`.
* **[PASS] M3 re-picking the row on screen costs nothing** — choosing the active burned-in track again produced no log line and no gap; playback ran on to 1m21s. It used to re-resolve the stream for a five-second round trip to where it already was.
* **[PASS] M4 turning a burned-in subtitle off, with the rest carried** — "None" restarted at 122.6s with `audioindex=2&subtitleindex=-1`, no `burnsubs`, and `transcode=1&bitrate=3` reproduced; the new payload reports stream 5 back at `DeliveryMethod: External`, i.e. no longer in the picture. The audio track survived a subtitle change, which is what stating every field on a restart buys.
* **[PASS] M5 audio on a direct play switches in place** — *3:10 to Yuma* (dts + ac3, DirectStream): the dialog opens preselected on the DTS-HD track being heard, and picking the commentary logged `audio -> jellyfin 2 (kodi 1)` with Kodi's `currentaudiostream` moving to ac3 and playback continuing through 46s. No restart, no toast. It used to re-resolve the stream for a track Kodi already held.
* **Incidental, and worth recording**: the first M1 attempt omitted `mediasourceid` from the play URL and the server silently ignored `subtitleindex`, answering with the profile's own default — §2.6 of the plan, reproduced live from the play route rather than from curl.

### External subtitle labels — sidecars as local files (2026-08-05)

Run on the `kofin-test` profile (Omega 21.3) against `jelly.konell.xyz`, branch `fix/external-sub-labels`.

* **[PASS] X1 the defect, and the fix** — *…And Justice for All* (DirectStream, one sidecar SRT) listed in Kodi as `name: "(External)", language: ""` before the change and `name: "English (External)", language: "eng"` after it. The empty label was not merely missing a language: Jellyfin serves the file as `Stream.subrip`, whose stem matches the video stream's own `/Videos/<id>/stream.mkv`, so Kodi stripped it as a redundant prefix and had nothing left.
* **[PASS] X2 cost** — the fetch is one round trip on the play route: `play … via DirectStream` at 17:18:27.383, `fetched 1 sidecar subtitle(s)` at 17:18:27.405. **22 ms**, against the 4.0 s baseline to first frame.
* **[PASS] X3 sidecars only** — *After Hours* transcoded with **28 embedded text subtitles and no sidecar**: nothing fetched, cache directory empty, and all 28 still attached by URL (labelled `Stream (External)`, which is what kofin's own stream dialog is for). Paying for 28 fetches before `setResolvedUrl` is the startup delay the design forbids.
* **[PASS] X4 the format conversion** — the file written is `English.eng.srt`, 128,891 bytes, fetched from the route asked for `.srt` rather than the `.subrip` the server offered. Kodi has no parser registered for `.subrip`.
* **Filename grammar**, measured by adding real files to a running playback: `English.eng.srt` → name "English (External)", language eng; `Commentary.eng.forced.srt` → forced flag set, token consumed; `eng.srt` → language but no name; `English SDH.eng.sdh.srt` → **`sdh` is not a flag** and leaks into the name. Hence name + language + `forced`, and nothing else.

## S-loops — healing-loop convergence (docs/healing-loops-plan.md; run 2026-08-06, kofin-test profile, Omega/MyVideos131, Jellyfin 10.11.11)

**S-loops-1 (F1, boxset shared member) — PASS.** Two throwaway collections sharing one movie ("Kofin Loop Probe A/B") created via `/Collections`; the incremental sync landed both with the classic mid-cycle shape (B stole the shared member last-wins, A's stored count one high). Restart 1: `boxset drift probe: 1 of 56 set(s) unhealthy` → the scheduled walk logged `healing boxset` for both sets and the summary `56 checked (54 unchanged, 2 healed)` — the predicted within-walk steal cascade — and the walk-end restamp left stored == measured for both (A=0, B=1). Restart 2: zero probe lines. Collections deleted server-side; removal echo swept references, sets rows and state rows; movie's `idSet` NULLed; baseline 54 sets / 54 states byte-consistent.

**S-loops-2 (F2, misattributed reference) — PASS, with a trigger finding.** True series pooling could not be manufactured on 10.11.11: a second library over the same paths reuses the same item ids (Jellyfin keys by path), and with distinct paths (symlinked copy, matching Tvdb ids, `EnableAutomaticSeriesGrouping` on both libraries, per-library and full scans) `/Shows/{id}/Seasons` still never returned a foreign `SeriesId` — the pool arm's trigger appears dormant against 10.11, so the two-order pooling legs rest on the L2 suite. The heal chain itself ran live against the legacy-poisoned shape (a real series' reference re-pointed at the wrong library in kofin.db): boot probe read `server:-4/+4` across the two libraries (the series' whole parent chain travels via the traversal) and scheduled the update; the prune spared all 4 chained ids and `_rehome_spared` re-homed exactly the series (season/episodes exempt) with the other library's prune already `missing:0` in the same pass; next boot fully silent, row verified re-homed. One incidental: restarting mid-UpdateLibrary parked the unfinished Music leg in sync.json and the next boot resumed it — the resumable queue behaving as designed.

**S-loops-3 (F5, node ordering) — PASS.** This profile exercises the degradation for real: `/Library/MediaFolders` 403s on every startup (non-admin token) and node generation runs from the views-only ordering. A RunScript harness drove the installed `Views.get_nodes()` with a whitelisted view (Recordings) removed from the in-memory `SortedViews` and the hash cleared: generation completed with no `ValueError` (the pre-fix crash site), the hash stamped, and both of the Mixed view's node folders carried the deterministic fallback `order="9"` (len(SortedViews)=8 + sorted-whitelist offset 1). A service bounce regenerated once with the true ordering (`order="8"`), and a second bounce logged `nodes ] unchanged (hash match)` — converged. (Harness artifact: `Views(None)` raises handled `NoneType` errors in the window-props leg; node generation is unaffected.)

F3's ladder is L1-only by design (clock-driven); F4 has no live surface beyond the field-list guard.

## 5. Performance baselines (record, don't guess)

Captured by the scenario scripts into `tests/live/results/` per run: initial full-sync wall time for the test set; catch-up latency for 100 mixed pending changes (per tier); addon HTTP request count per scenario (debug-log grep); Kodi-start→library-browsable delta; S2.5 download counts per tier. Regressions between runs of the same scenario on the same data are release blockers.

## 6. Cadence

* Every commit: L0 + L1 (+ L2 when writers changed) via tox.
* Before merge to main: smoke set — S1.4, S1.5, S2.1, S2.3.
* Per release: full catalog for the phases shipped so far.
* On Kodi or Jellyfin version bumps: L2 against the new schema first, then the live smoke set.
