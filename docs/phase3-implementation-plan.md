# plugin.video.kofin — Phase 3 implementation plan

Date: 2026-07-17. **Rev. 2026-07-18** — replaces the `service.upnext` hand-off with a self-contained kofin dialog (see §2, §8.2). Operationalizes build phase 3 from `rewrite-research.md` §11 — the **player features**: media-segment skipping, next-episode (**Play Next**) coordination, and extras (native for movies, browseable for TV). The report governs design (§8.3); **this plan deviates from report §8.2** (the Up Next hand-off) in favor of a kofin-owned dialog — the report and `testing-plan.md` S3.3 get a matching update per §8. The fork (`ref/jellyfin-kodi`, branch `combined/syncplay-sync`) is the source for the segment/skip code, which ports; extras are new code the report specifies. Test gates reference `testing-plan.md` (S3.x). Phases 1 and 2 are complete, merged to `main`, and live-verified (S1.x, S2.1–S2.5/S2.8/S2.9 pass on the `kofin-test` profile).

**Phase 3 deliverable**: while an episode plays, kofin fetches its Jellyfin media segments and skips or offers to skip Intro/Credits/Recap/Preview/Commercial per per-type settings; at the Credits/Outro segment it raises a single kofin-owned overlay offering **Skip Outro / Play Next / Close**, and for content with no segment data it shows its own near-end **Play Next** prompt — no dependency on `service.upnext`; movies with special features get native Kodi **Extras** entries in the library, and shows with specials get a browseable Extras node. Everything is service/plugin-side — no writer changes to the core sync tables except the movie extras asset rows (which reuse the `videoversion` machinery phase 2 already drives).

---

## 1. Scope

**In**: segment fetch + per-type skip engine (`service/segments.py` checker + player `check_skip_segments`, ported and **hardened for timing** — sub-second tick, boundary-crossing detection, warm fetch; see §2); a single kofin-owned **multi-action overlay** (the *only* custom window in kofin, generalized from the fork's `dialogs/skip.py` + `script-jellyfin-skip.xml`) rendering **Skip Outro / Play Next / Close** at the Credits segment and **Skip \<type> / Close** for other segments; kofin's own next-episode resolution + **Play Next** start-path, plus a near-end Play Next prompt for content with no segment data (no `service.upnext` dependency); movie extras synced as native Kodi video assets (`videoversion` rows, `itemType = EXTRA`, per-schema constant), incremental via the same added/updated events; a TV-extras browse node + "Browse extras" context item; the Media Segments settings group; playback of extra items through the existing `play` handler; S3.x live gates.

**Out (later phases)**: SyncPlay (`syncplay/` package — phase 4); KofinSyncQueue and tier-1 (phase 5); plugin-free fallback (phase 6); Kodi "versions" sync of Jellyfin MediaSources (YAGNI §9 — extras exercise the asset tables, native versions UI is a clean later addition); **any `service.upnext` integration, vendored or via its signal API — dropped entirely; kofin owns the next-episode dialog (§2)**; theme media.

## 2. Decisions locked in phase 3

* **Segment skip = ported behavior, coordination + timing = new.** The fork's segment checker, `check_skip_segments`, `_process_segment` (EOF clamping), the per-type mode map, and the `SkipDialog` overlay port with mechanical adaptation (imports, settings ids, addon id, skin path). What changes: (a) the checker is **hardened for timing** (bullet below); (b) the credits interaction becomes a **kofin-owned unified dialog** (§8.2) instead of the fork's stack-a-popup-and-signal-upnext behavior (`player.py:648-652`). No restyling of the segment math itself.
* **One dialog, kofin-owned (supersedes report §8.2).** kofin never hands the moment to `service.upnext`; it renders one multi-action overlay and decides its buttons from the segment + next-episode state. On playback start, fetch segments and (for episodes) resolve the next episode. Decision at the Credits/Outro crossing:
  * next episode exists, not in a SyncPlay group → **[Skip Outro] [Play Next] [Close]**.
  * no next episode (season/series finale) or inside a SyncPlay group → **[Skip Outro] [Close]** (Play Next withheld — the group queue is authoritative, per the fork's `next_up` guard `player.py:297-301`).
  * `skipCreditsMode = Auto` still auto-seeks past credits; the overlay only appears in Ask mode, or whenever a Play Next is on offer.
  Intro/Recap/Preview/Commercial always render **[Skip \<type>] [Close]**, never Play Next. For content with **no Credits segment** but a next episode, kofin shows the same overlay as a **near-end Play Next prompt** at `end − playNextLeadTime` (**[Play Next] [Close]**, no Skip). No segment data and no next episode → nothing.
* **Event-driven overlay, not a busy-loop.** The fork's `_monitor_skip_dialog` (`player.py:773-807`) polls in a loop that blocks the checker thread (the cause of the fork's `fix/segment-checker-shutdown-hang` branch). kofin drives the overlay's lifetime from the segment tick (open at the crossing, auto-close past `end`, close on button press) — no second monitor thread, no busy-wait. The checker thread stays decoupled from the player (report's "keep the fork's decoupled segment checker"). **Correction (S3.2 live):** the overlay is created on the checker thread, so Kodi delivers its `onInit`/`onClick`/`onAction` callbacks to that thread's invoker only when the thread yields to Kodi — the checker's between-tick wait therefore must be `xbmc.Monitor().waitForAbort` (Kodi-aware), not `threading.Event.wait` (pure Python), or the buttons never receive input. This is the one thing the fork's monitor loop was quietly providing (it pumps `waitForAbort`); we keep the single tick-driven thread but make its wait Kodi-aware. No second thread reintroduced.
* **Timing robustness (new — the fork mis-fires).** Upstream ticks at **1 Hz** (`segments.py:39`) and tests integer-truncated membership `start <= int(getTime()) <= end` (`player.py:31,611`), so a segment can be missed entirely (short window, or late-arriving fetch) and, once missed, the permanent `skip_prompted` set (`player.py:558,640-643`) never re-offers it. kofin fixes this: **(a)** tick at **0.25 s** on `float(getTime())`; **(b)** detect **boundary crossing** — fire when `prev_pos < start <= now` *or* `start <= now <= end` (the second clause catches seeks and late-loaded segments where we are already inside), so a coarse/late poll cannot step over a boundary; **(c)** **pre-arm** the next sorted boundary (O(1) compare per tick); **(d)** **warm the fetch** — piggyback `media_segments` on the play-path item fetch and gate the checker's first arm on segments-loaded, killing the t≈0 Intro race; **(e)** **recoverable dedup** — re-arm a segment when the user seeks back out and in; **(f)** **settle after an auto-skip seek** (skip one tick / wait for `getTime()` to pass the target) so post-seek time-lag does not re-trigger.
* **Play Next uses kofin's own play path (no IPC, no upnext).** The overlay's Play Next button resolves the next episode up front (fork `next_up` adjacency, `player.py:309-323`, ported to `core/api.py`) and, on click, starts it through the normal `play` handler by kofin id (reports `PlaySessionId` etc. unchanged). Dropping `service.upnext` removes the `upnext_data`/`upnextprovider` signal round-trip and the `notification_time`/`notification_offset` contract entirely — `core/ipc.py encode_hex` is not needed for this feature. (Aside: the report path set `notification_time` = credits start, which upnext interprets as *seconds-before-end* — `api.py:186-193` — and would have mis-fired; moot now that we do not use it.)
* **Movie extras are native, reusing phase-2 asset machinery.** Jellyfin `SpecialFeatureCount > 0` → fetch `/Users/{u}/Items/{id}/SpecialFeatures` → one `files` row + one `videoversion` row per feature (`media_type='movie'`, `idMedia = idMovie`, `itemType = EXTRA`, `idType` = a named `videoversiontype` mapped from Jellyfin `ExtraType`), with a `plugin://plugin.video.kofin/…mode=play&id=<extraId>` path. The `videoversion` add/delete/idempotency already works (phase 2 writes the main version and the L2 suite covers it); extras are additional rows on the same tables. **Schema constant: EXTRA = 1 on Omega**; Piers (EXTRA = 2) stays refused by the schema gate until its fixtures land, so only the Omega constant is wired now — but `schema.py` carries the per-version value so Piers is a data change, not a code change.
* **TV extras are a plugin browse** (Kodi has no native TV extras): a dynamic Extras node for series/seasons whose DTO reports `SpecialFeatureCount > 0`, plus a context item on library shows, both routing to a `mode=extras` listing over the same `SpecialFeatures` endpoint. No DB writes for TV extras — they are a live listing, like the phase-1 browse nodes.
* **Extras never gate the core sync.** A movie whose SpecialFeatures fetch fails still syncs (the extra request is best-effort, one per movie that advertises `SpecialFeatureCount > 0`, incremental via the same added/updated events). Degrade-not-die, as phase 2.
* **Trailers unchanged.** The existing local-trailer / YouTube handling (phase 2 `trailer()` in the movie/tvshow writers) stays; extras are a separate asset path.
* **Strings**: 30450–30499 Media Segments settings + skip/Play Next UI; 30500–30549 extras (node labels, context items).

## 3. Port map (fork `jellyfin_kodi/` → kofin)

| Source (fork) | Target | ~Lines | Adaptation notes |
|---|---|---|---|
| `segments.py` | `service/segments.py` | 45 | `SegmentChecker` thread; `settings()` → kofin settings, `window`/exit checks → kofin state; `waitForAbort` loop **retimed 1 Hz → 0.25 s** with pre-armed boundary + warm-fetch gate (§2 timing robustness) |
| `player.py` segment methods (`check_skip_segments`, `_process_segment`, `_get_segment_skip_mode`, `_handle_skip_segment`, segment-type map) | `service/player.py` (additions) | 250 | EOF clamping and per-type mode logic verbatim; membership test → boundary-crossing; `_monitor_skip_dialog` busy-loop replaced by tick-driven open/close; credits interaction rewritten to the kofin-owned dialog per §8.2 |
| `player.py` `next_up()` adjacency (next-episode resolution only) | `service/player.py` (`resolve_next_episode`) | 40 | drop the `upnext_data`/`event(hexlify=True)` send; keep the `get_adjacent_episodes` lookup (`player.py:309-323`) for the next kofin id + label/art; SyncPlay-group guard kept |
| `dialogs/skip.py` (`SkipDialog`) | `plugin/skip.py` (multi-action) | 120 | `WindowXMLDialog` port generalized to N buttons (Skip / Play Next / Close) with per-button action flags; addon id, skin filename, string ids |
| `resources/skins/default/1080i/script-jellyfin-skip.xml` | `resources/skins/default/1080i/script-kofin-skip.xml` | 90 | rename; **third button** (Play Next, id 3014) in the horizontal grouplist, shown/hidden by a window property; native Estuary-ish styling, stock textures only (no addon-branded art, per phase-2 §2) |
| `jellyfin/api.py` `MediaSegments/{id}`, `SpecialFeatures` | `core/api.py` (`media_segments`, `special_features`) | 25 | typed additions to the phase-2 `Api`; user-scoped SpecialFeatures |

New code (report §8.3, no fork source): the near-end Play Next scheduler (`service/player.py`, ~30 — fire the overlay at `end − playNextLeadTime` when there is no Credits segment); extras writer additions (`sync/writers/movies.py` extras pass + `sync/kodidb/movies.py` asset helpers, ~150), `ExtraType → videoversiontype` map + `EXTRA` schema constant (`sync/schema.py`, ~30), TV-extras browse (`plugin/browse.py` `mode=extras` + context item, ~120), extras play-path handling in `plugin/play.py` (~20), settings XML/strings (~250), fixtures + tests (~400).

## 4. Settings spec

### Media Segments group (Playback tab, `category` TBD, 30450+)

The Playback tab itself is a phase-3 addition (phase 2 shipped Account/Transcoding/Library/Sync). Minimum for segments:

| id | type/control | notes |
|---|---|---|
| `mediaSegmentsEnabled` | bool, default **true** | master toggle; gates the checker thread and the API fetch |
| `skipIntroductionMode` | spinner Off/Auto/Ask, default **Auto** | 0/1/2; Auto seeks past, Ask shows the skip button |
| `skipCreditsMode` | spinner Off/Auto/Ask, default **Ask** | Ask → the unified overlay; Auto → auto-seek past credits (Play Next still offered if enabled) |
| `skipRecapMode` | spinner Off/Auto/Ask, default **Ask** | |
| `skipPreviewMode` | spinner Off/Auto/Ask, default **Off** | |
| `skipCommercialMode` | spinner Off/Auto/Ask, default **Auto** | |
| `playNextEnabled` | bool, default **true** | master toggle for the Play Next button + the near-end prompt |
| `playNextLeadTime` | int seconds, default **30** | content with **no Credits segment**: show the near-end Play Next prompt this many seconds before end (clamped on short items) |
| `playNextAutoplay` | bool, default **false** | when on, the Play Next overlay counts down and auto-starts the next episode; when off (default) the user must click Play Next |

Extras: no user setting in v1 (always synced for movies, always browseable for TV). An `enableExtras` bool can be added if the SpecialFeatures request cost proves noticeable on very large movie libraries — defer until measured (§7).

The report (§7 line 232) also lists resume/`markPlayed`/offer-delete/cinema-mode groups for the Playback tab; those are Playback-tab scope but not phase-3 *player-feature* work — land the tab shell here, fill the non-segment groups opportunistically (they are settings + small handlers, no new subsystems).

## 5. Work breakdown (ordered; each step lands green on L0/L1/L2)

1. **Api + Playback tab shell** (S): `Api.media_segments(item_id)` and `Api.special_features(item_id)`; the Playback settings category with the Media Segments group + strings. *DoD*: L0 green; settings render (S1.1-style screenshot); `media_segments` returns the typed shape against a segments-capable test item.
2. **Segment engine transplant + timing hardening** (L): `service/segments.py` checker (0.25 s tick, `float` time, pre-armed boundary, warm fetch) + player segment methods + `plugin/skip.py` + the skip skin xml. Wire the checker to start/stop with playback (phase-1 player lifecycle). *DoD*: L1 on `_process_segment` (EOF clamp: a segment ending past runtime never seeks past it; recoverable de-dup; mode 0 skips nothing) and on boundary-crossing (`prev<start<=now` fires; already-inside fires); S3.1 (auto-skip) and S3.2 (button) live on the test show, including a **short intro at t≈0** (fetch-race gate) and a **coarse-seek-over** case.
3. **Unified Play Next dialog** (M): generalize `plugin/skip.py` to the multi-action overlay; `resolve_next_episode` adjacency; wire Play Next → `play` by kofin id; the near-end prompt scheduler for no-segment content; the button-decision logic. *DoD*: L1 on the decision matrix (segment type × next-episode yes/no × movie/episode × group/no-group × segment-present/absent → which buttons, or near-end prompt, or nothing); S3.3 live (no `service.upnext` installed).
4. **Movie extras (native)** (M): `ExtraType → videoversiontype` map + `EXTRA` constant in `schema.py`; writer pass that, on `SpecialFeatureCount > 0`, fetches SpecialFeatures and writes one asset (`files` + `videoversion`) per feature with a plugin play path; removal cleans the asset rows (extend the movie `remove` cascade); `play` handles extra ids. *DoD*: L2 fixture — a movie with two special features writes two `videoversion` rows with `itemType = 1` (Omega) and sensible `videoversiontype` names; idempotency (second write byte-identical); removal leaves zero orphan `videoversion`/`files` rows. S3.4 live (Extras button on the info dialog; an extra plays + reports).
5. **TV extras (browse)** (S): `mode=extras` listing over SpecialFeatures for a series/season; an Extras node in the tvshows node menu when the view has any specials; "Browse extras" context item on library shows (addon.xml visibility extended to `DBTYPE=tvshow/season`). *DoD*: L1 on the listing builder; S3.5 live.
6. **Live gates** (M): S3.1–S3.5 on the `kofin-test` profile against a segments-capable show and the two-extras movie in the test set; screenshots for the skip button and the Play Next dialog timing; masking grep clean over a segment+extras playback session.

Steps 1→2→3 are sequential (each builds on the last); 4 and 5 can overlap with 3 (independent code paths); 6 gates everything.

## 6. Test gates (from `testing-plan.md` §S3)

* **S3.1 segment auto-skip**: seek to shortly before a known intro; position jumps past the segment end within the (0.25 s) poll interval; notification shown; an EOF-clamped segment never seeks past runtime; an Intro at t≈0 still fires (warm-fetch gate); a fast seek that steps *over* a segment still auto-skips (crossing detection).
* **S3.2 skip button**: button mode shows the overlay at the segment crossing (screenshot); pressing Skip seeks to segment end; ignoring it auto-closes past segment end; never two overlays at once.
* **S3.3 unified Play Next dialog** (no `service.upnext` installed): an episode with a Credits segment and a next episode shows **one** kofin overlay at the credits crossing with **Skip Outro / Play Next / Close**; Play Next starts the next episode through kofin; Skip Outro seeks past credits; the season's last episode shows **Skip Outro / Close** only; a show with **no** Credits segment shows the **near-end Play Next prompt** at `end − playNextLeadTime`; inside a SyncPlay group, Play Next is withheld. Never two overlays at once.
* **S3.4 movie extras**: after sync, the two-extras movie has `videoversion` rows (itemType per schema) with sensible names; the info dialog shows the Extras button; an extra plays and reports.
* **S3.5 TV extras**: the show with specials exposes the Extras browse node; entries play.

The test set needs one **segments-capable show** (Intro Skipper-analyzed or chapter-based) and one **two-extras movie** — both are already called out in `testing-plan.md` §1 as required test media; coordinate with Conor to add them to the test libraries before S3.x (the current live set — Movies/Documentaries/Music-Alt — has neither guaranteed).

## 7. Risks / watch items

* **Segment-timing accuracy (the core reliability goal)**: upstream fires late or never because of 1 Hz + integer membership (`segments.py:39`, `player.py:31,611`). The 0.25 s tick + boundary-crossing + warm-fetch (§2) are the fix; S3.1/S3.2 must confirm on a **short intro at t≈0** and a **seek that steps over** a segment, under real network latency to Jellyfin (the media-segments fetch is a live GET). If a slow fetch still loses a t≈0 Intro, gate playback-start-arming on the fetch completing (short bounded retry) rather than best-effort.
* **Near-end prompt vs. EOF**: for no-segment content the Play Next prompt fires at `end − playNextLeadTime`; on very short items (`runtime < playNextLeadTime`) clamp the lead so it still appears, and ensure it closes cleanly on natural EOF if untouched.
* **Custom skin window portability**: `script-kofin-skip.xml` is the only custom window; some skins/resolutions have historically fought `WindowXMLDialog` overlays. Keep it minimal (a label + two buttons, stock textures), and test under the box's actual skin (contuary) as well as Estuary — a skip button that renders under Estuary but not the daily skin is a real failure.
* **Extras request cost**: one SpecialFeatures fetch per movie advertising specials. On the 1758-movie library that is bounded by how many actually have extras (likely a small fraction), but measure on the first full sync with extras enabled before deciding whether `enableExtras` needs to exist (§4). Incremental syncs pay it only for changed items.
* **EXTRA constant drift on Piers**: `itemType` for EXTRA renumbers 1→2 in Kodi migration 134 (Omega→Piers). The gate refuses Piers today, so only EXTRA=1 is exercised; when Piers fixtures land (phase-2 hardening), the L2 extras test must assert `itemType = 2` there. Keep the constant in `schema.py` keyed by version, never inlined in the writer.
* **Segment checker vs SyncPlay**: the "inside a SyncPlay group → withhold Play Next" hook (Skip Outro still works) is stubbed in phase 3 (SyncPlay is phase 4). Ensure the stub reads a state flag that phase 4 will set, so wiring SyncPlay later does not require touching the segment engine.

## 8. Exit checklist

* L0 green (mypy relaxed only over `kofin/sync/**` as phase 2; new `service`/`plugin` code is strict); L1/L2 green including the extras asset idempotency + removal-integrity invariants, the **Play Next decision matrix**, and the **boundary-crossing timing** cases.
* S3.1–S3.5 pass on the test profile with evidence in `tests/live/results/`; skip-button and Play Next dialog timing screenshots captured under both Estuary and skin.contuary.
* Masking grep clean over a segment+extras playback session.
* Report/testing-plan updated in the same commit as any deviation (as phase 2's S2.9 widget-policy revision was) — **including report §8.2 and `testing-plan.md` S3.3**, which this plan supersedes with the kofin-owned dialog.
* No regression in the phase-2 gates: a quick S2.1 re-sync of the test set still matches counts (the extras writer additions must not perturb the main-version rows — the A/B `ab_diff.py` is the guard).
