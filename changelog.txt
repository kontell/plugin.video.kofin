v0.24.0
- SyncPlay tunes live channels and catchup programmes together. Needs pvr.kofin 22.15.0
- Live channels converge on the broadcast's own clock through Tempo; a member ahead of the group waits for it. Needs inputstream.tempo 22.5.0 or 21.5.0
- A live channel plays the provider's stream directly when the server allows it
- A recording played from the Recordings window reloads through Tempo so it can fine sync

v0.23.0
- Find servers on the local network
- Downloads show how much space they take, including artwork and metadata
- A music download exports artist and album metadata with it
- SyncPlay groups can follow another add-on's content. Needs the SyncPlay v2 plugin with external-content support
- Kodi 22.0 beta 2 syncs again
- A playlist download announces once, not once per album
- A downloaded song no longer offers to remove itself after playing
- The Movies, Shows and Music folders stay when their last download is removed

v0.22.0
- Fine sync supported for a transcoded stream
- Add-on refactored
- A spectator's own playback is no longer driven by the group
- Kodi's audio/video queue is restored after an interrupted SyncPlay session
- A library that fails to sync no longer abandons the ones after it
- An item deleted on the server mid-sync is skipped, not written
- Removing an episode takes its resume bookmark with it
- A sync page that fails to fetch is retried, not dropped
- A whitelisted library kind with no nodes is skipped, not fatal
- The IPC nonce is no longer written to the log
- Kodi's "Manage versions" picker no longer lists every film in the library
- A user whose library access was withdrawn no longer has every synced library deleted from Kodi
- Cast is written correctly after Kodi's own "Clean library" has run
- Leaving a SyncPlay group during shutdown no longer holds Kodi past its five-second stop grace when the server has gone away
- The widget-refresh fingerprint reads kofin's mapping table about forty times faster per row
- A music library whose item count answers zero no longer crashes its sync
- Maximum streaming bitrate offers 0.5, 0.75, 1 and 2 Mbit/s, matching the download cap
- "Jellyfin actions" and "Play with transcoding" no longer appear on other add-ons' listings
- Deleting a download says so: the item by name, or a count when a season or album goes at once
- A download that finishes or is deleted updates the widgets, so the downloaded badge appears and clears without waiting for something unrelated to change
- An artist that arrives after its tracks re-credits them

v0.21.1
- Joining a group in progress starts at the group's position, not double it
- A small slip on resume is smoothed by fine sync instead of seeked

v0.21.0
- SyncPlay keeps a group together between commands, not only at them: a member that has slipped plays faster or slower to catch up. Needs inputstream.tempo 21.4.1 / 22.4.1
- inputstream.tempo is a recommended optional dependency
- Two sliders: the largest slip closed by speed (2.5s), and the fastest correction (25%)
- On Kodi 22 a session shortens the audio/video queue to 1s and restores it after, a crash included
- Audio passthrough is suspended for a session
- A member that keeps drifting is told once and left on command-only sync

v0.20.1
- Cast appears in search results and in a person's list of films
- The cast setting says which listings it covers, and why whole-library ones never will

v0.20.0
- Reset resume position on items browsed through the add-on
- An item finished on another device no longer shows as resumable
- Recently added albums lists what actually arrived
- Play all and Shuffle on albums, artists, genres and playlists
- "Play with transcoding" is hidden where it could only force a transcode
- Optional cast in the shorter add-on listings, off by default

v0.19.1
- Lyrics directory padded so Kofin Lyrics draws them correctly

v0.19.0
- SyncPlay holds a group together: starting, pausing, resuming and seeking land every member within about a fifth of a second
- Drift correction removed, and the setting says why: it needed "Sync playback to display", which costs up to 4.3% drift
- A transcoding member reloads at a group seek instead of being stranded
- A stale timer no longer drops a member from its group after a seek
- A sync that fails at startup no longer floods the screen with toasts
- Music libraries start syncing sooner: 13 seconds off startup on a slow box
- Waking from sleep runs one catch-up, not two

v0.18.1
- Unticking AV1 in the transcoding settings turns it off

v0.18.0
- Search the server: films, shows, episodes, albums, songs and people
- More ways in: Years, Tags, A-Z, Unwatched, Album artists, Last played, Top 100 songs
- Playlists show a playlist icon
- A kofin path ending in a slash no longer comes up empty
- Syncing music no longer hides Kodi's own Genres, Artists, Albums and Songs
- Kodi 22 beta builds sync again
- Each library appears on the home screen as it finishes, not when the whole sync does
- A first sync no longer leaves a library's home row blank

v0.17.0
- Skins can show SyncPlay and Who's watching? on the home screen
- Translated into 26 languages
- Stopping a watch-together group no longer freezes Kodi
- A downloaded item plays from disk on every client in a group
- "Who's watching?" gains an All row and can be turned off
- Removing a download no longer asks for confirmation
- SyncPlay drift correction is off by default

v0.16.0
- Joining a group already playing no longer pauses it for everyone. Needs the SyncPlay v2 plugin 10.11.0.2
- A scheduled group start seeks before the clock fires, so clients start together
- SyncPlay speaks protocol v2 where the server has the plugin: clock sync on its own connection, versioned updates, snapshot recovery

v0.15.2
- Films, shows and episodes are stored under every external id the server has
- Updating the add-on while Kodi is running no longer interrupts a sync

v0.15.1
- Updating the add-on while Kodi is running no longer leaves the server looking unreachable

v0.15.0
- The skip prompt shows how much it will skip, and steps out of the way part-way through
- Recap, Preview and Commercial no longer skip by default
- Two intro-detection plugins no longer produce two prompts
- Catch-up ignores libraries you have not chosen to sync. Needs Kofin Sync Queue 10.11.0.1
- Items that could not be added are no longer announced or queued for download
- Clean databases offers to delete your downloaded files, and defaults to doing so
- "Delete downloads you have not watched" is now "Delete stale downloads"

v0.14.0
- Jellyfin's audio and subtitle preferences are editable from the Account tab
- The Kofin music node has a folder per library, and Downloaded music splits the same way
- Downloads you never started can be cleared after a number of days
- Menus and playback respond quicker: most listings open about twice as fast
- The backdrop takes a fifth of the disk space it used to
- A queued download starts in seconds, and eight further downloader fixes
- Artists no longer collect duplicate or stray discography entries
- Subtitles on a transcode arrive without restarting playback
- The service recovers on its own when a thread dies or an update stalls

v0.13.1
- Music downloads are several times faster, with one notification per album
- The Downloads settings page appears as soon as downloads are enabled
- A downloaded show or album offers Remove download, with your free space shown
- "After watching a download" is one toggle, and it starts off
- Downloaded views appear straight away, with their own icon

v0.13.0
- Download for offline playback: movies, episodes and music
- Downloads play with the server away; watched state and resume replay on reconnect
- Optional transcoded downloads convert only what breaks a limit
- Automatic downloads: next episodes, new items, show subscriptions, watched cleanup
- Kofin can offer to remove a download once you finish it
- Optional NFO and artwork export
- Downloaded playbacks report to the server

v0.12.0
- Browsing large libraries is around nine times faster
- Playback starts sooner, and a transcode with subtitles no longer stalls
- Library nodes are grouped by type, favourites after them
- Delete is offered only to accounts the server lets delete
- Extras moved into Jellyfin actions
- Resume offset moved to Settings / Playback
- The image setting that did nothing is gone
- A slow server no longer removes libraries or stalls shutdown

v0.11.1
- The subtitle your Jellyfin profile asks for is the one that plays
- "Play with transcoding" starts while something else is already playing
- Shows that live in more than one library are filed under the right one
- A film in two collections no longer reads as permanently out of date
- Library nodes generate when the server answers incompletely
- A library that fails to sync notifies once, and the retries back off

v0.11.0
- "Clean databases" removes every trace of Kofin and jellyfin-kodi
- Critic ratings are imported, and can replace the community rating
- Media segment skipping is off by default
- The stream menu marks the track playing, and can turn one off
- Subtitle files beside your media show their language
- Kofin's playlists moved into a "Kofin" folder
- The add-on's fanart no longer sits behind items with no artwork of their own
- Moved off the Jellyfin API routes 10.11 no longer documents

v0.10.0
- Kofin has a backdrop: your server's own splashscreen
- Home-screen widgets redraw only when what they show has changed
- Bursts of small changes fold into one refresh, aimed at the right library
- Newly synced content appears on the home screen without a restart
- Collections repair themselves
- A collection is no longer emptied by a server answering that it has no members
- The "pause sync while the screensaver is on" setting is gone

v0.9.0
- Kodi 22 (Piers) is supported
- Chapter images from Jellyfin appear in Kodi's chapter list
- Library nodes sit under a single "Kofin" parent, with a "Who's watching?" node
- New content on the server raises a notification, one per type
- Cast is back in the info dialog for shows and episodes
- Watched flags follow Jellyfin's Played state, not the play count
- HDR flags show on dynamically browsed items
- Stock icons in the movies, shows and music sub-sections
- Music playlist lines report to Jellyfin and pick up lyrics

v0.8.0
- Continue watching leads the add-on's own listing
- The audio and subtitle chooser appears for every transcode that has something to offer
- "Mark as watched on Jellyfin" no longer duplicates Kodi's own in a synced library
- Music playlists show artist, album and title, and report to the dashboard

v0.7.0
- Music is no longer re-encoded by "Force remux" or "Force transcode"
- Your Jellyfin music playlists keep themselves up to date, re-read every fifteen minutes
- Films held in more than one version appear as one entry with its versions under it
- Audio and subtitle tracks can be changed during a transcode, and picture subtitles burned in
- The transcode context item offers the resume prompt, and re-encodes
- Movie extras show their own running time
- The "Who's watching" choice survives a Kodi restart

v0.6.1
- Lyrics are drawn by the new Kofin Lyrics add-on, installed alongside Kofin
- Lyrics settings are now a choice: off, Kofin Lyrics, or your own lyrics add-on

v0.6.0
- Lyrics held on your server are handed to Kodi as each track starts, timings included. Needs an add-on that displays them

v0.5.0
- Resume starts where Kodi's prompt said it would, with a rewind slider (10 seconds)
- Kodi's "Mark as watched", "Mark as unwatched" and "Reset resume position" reach the server
- Resuming into the middle of an intro no longer flashes the skip dialog
- The information dialog opens for items in Kofin's own listings
- Jellyfin actions are no longer offered twice in the context menu
- Notifications carry the right icon, and two that described the wrong problem are fixed
- The music transcoding settings take effect
- A song that names no artist is credited to its album artist
- Sync no longer removes library rows it should have kept
- Orphaned seasons and episodes heal themselves
- A failed settings read no longer looks like every library was deselected
- New on Advanced: a Who's watching shortlist, delete without confirmation, delete after watching
- Force direct play is on by default, and AC3 is no longer a transcode target

v0.4.0
- Fixed episodes of an unrelated show being deleted when a season was removed
- Library updates settle instead of rewriting every season on every run
- Seasons with no folder of their own are tracked like any other
- Kofin compares each library against the server at startup and repairs what is missing
- "Some items did not sync" is no longer reported for folders that were never library content
- Problems Kofin resolves itself are logged, not notified
- Note: the first update after upgrading is a large one

v0.3.0
- Force transcode re-encodes instead of passing the original through
- Transcoded audio bitrate scales with the streaming limit
- Added E-AC3, and 480p/1440p maximum resolutions
- "Play with transcoding" is hidden when no context bitrates are set
- Fixed music sync failing, reporting false success, and hanging Kodi on quit
- Sync resumes after a dropped connection and catches up; the startup delay setting is gone
- Items downloaded but never written are recovered
- A large catch-up sync no longer grows unbounded in memory
- Faster music sync
- Repairing a library asks once, and sync progress no longer misreports
- Optional notifications when the server connects, disconnects or restarts
- Fixed a real artist occupying Kodi's blank-artist slot

v0.2.0
- Added the "Jellyfin actions" context menu

v0.1.0
- First release
